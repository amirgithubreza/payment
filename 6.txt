
<!DOCTYPE html>
<html lang="fa" dir="rtl">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>پرداخت | آموزشگاه پیشتاز</title>

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
    <link rel="stylesheet"
        href="https://cdn.jsdelivr.net/npm/persian-datepicker@1.2.0/dist/css/persian-datepicker.min.css" />

    <style>
        /* ===== RESET & ROOT ===== */
        :root {
            --purple: #46038b;
            --navy: #154894;
            --violet: #46077c;
            --cyan: #00fcff;
            --lavender: #ecdfff;
            --green: #1e7c00;
            --green-light: #d1fae5;
            --bg: #f8f8f8;
            --border: #e5e5e5;
            --border-light: #f1f5f9;
            --text: #1a1a2e;
            --text-mid: #444;
            --text-mute: #888;
            --danger: #dc2626;
            --danger-light: #fee2e2;
            --radius: 13px;
            --radius-lg: 18px;
            --radius-xl: 22px;
            --shadow: 0 4px 16px rgba(70, 3, 139, .07);
            --shadow-md: 0 8px 28px rgba(70, 3, 139, .10);
            --shadow-lg: 0 16px 48px rgba(0, 0, 0, .12);
            --transition: .2s cubic-bezier(.4, 0, .2, 1);
        }

        @font-face {
            font-family: 'YekanBakhFaNum';
            src: url('https://cdn.jsdelivr.net/gh/rastikerdar/vazir-font@v30.1.0/dist/Vazir-Regular.woff2') format('woff2');
            font-weight: 400;
        }

        @font-face {
            font-family: 'YekanBakhFaNum';
            src: url('https://cdn.jsdelivr.net/gh/rastikerdar/vazir-font@v30.1.0/dist/Vazir-Bold.woff2') format('woff2');
            font-weight: 700;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'YekanBakhFaNum', sans-serif;
            background: var(--bg);
            color: var(--text);
            direction: rtl;
            line-height: 1.7;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .checkout-wrap {
            flex: 1 0 auto;
        }

        .footer {
            flex-shrink: 0;
        }

        ::-webkit-scrollbar {
            width: 6px;
        }

        ::-webkit-scrollbar-track {
            background: #f1f1f1;
        }

        ::-webkit-scrollbar-thumb {
            background: var(--purple);
            border-radius: 10px;
        }

        /* ===== HEADER ===== */
        .navbar {
            width: min(1400px, 92%);
            margin: 18px auto 0;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 10px;
        }

        .nav-menu {
            list-style: none;
            display: flex;
            gap: 12px;
            flex-wrap: wrap;
        }

        .nav-menu a {
            text-decoration: none;
            color: #5f5f5f;
            padding: 8px 15px;
            border-radius: 20px;
            transition: 0.3s;
            font-size: 17px;
            font-weight: 500;
            cursor: pointer;
        }

        .nav-menu a.active {
            background: var(--purple);
            color: #fff;
        }

        .nav-menu a:not(.active):hover {
            color: var(--purple);
            background: var(--lavender);
        }

        .header-left {
            display: flex;
            align-items: center;
            gap: 15px;
            flex-wrap: wrap;
        }

        .login-box {
            width: 175px;
            height: 43px;
            background: var(--purple);
            border-radius: 30px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            color: #fff;
            font-size: 16px;
            font-weight: 700;
            position: relative;
            cursor: pointer;
            transition: all var(--transition);
            box-shadow: 0 4px 14px rgba(70, 3, 139, .25);
            border: none;
            font-family: inherit;
        }

        .login-box:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 24px rgba(70, 3, 139, .35);
        }

        .login-box span {
            margin-right: 15px;
        }

        .arrow-circle {
            width: 43px;
            height: 43px;
            background: #fff;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--purple);
            position: absolute;
            right: -10px;
        }

        .arrow-circle .circle {
            background: var(--purple);
            width: 40px;
            height: 40px;
            border-radius: 50%;
            transform: translateX(2px);
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .arrow-circle .circle i {
            font-size: 20px;
            color: #fff;
            transform: translateX(6px);
        }

        .cart-box {
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            margin-left: 20px;
            color: var(--text-mid);
            font-size: 22px;
            transition: all var(--transition);
            border-radius: 50%;
            position: relative;
            background: none;
            border: none;
        }

        .cart-box:hover {
            color: var(--purple);
            background: var(--lavender);
        }

        .cart-box .badge {
            position: absolute;
            top: -2px;
            left: -2px;
            width: 18px;
            height: 18px;
            border-radius: 50%;
            background: #ff3000;
            color: #fff;
            font-size: 9.5px;
            font-weight: 800;
            display: flex;
            align-items: center;
            justify-content: center;
            border: 2px solid #fff;
        }

        .search-container {
            display: flex;
            align-items: center;
            justify-content: flex-end;
            gap: 8px;
            position: relative;
            flex-direction: row-reverse;
        }

        .search-icon {
            cursor: pointer;
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            background: transparent;
            border-radius: 50%;
            transition: all 0.3s ease;
            flex-shrink: 0;
            z-index: 2;
            color: var(--text-mid);
            font-size: 19px;
            border: none;
        }

        .search-icon:hover {
            background: var(--lavender);
            color: var(--purple);
        }

        .search-input-wrapper {
            position: relative;
            display: flex;
            align-items: center;
        }

        .search-input {
            width: 0;
            opacity: 0;
            border: none;
            outline: none;
            padding: 12px 40px 12px 15px;
            background: #fff;
            font-size: 14px;
            color: #2c2c2c;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            border-radius: 30px;
            border: 2px solid #e8e0ff;
            box-shadow: 0 2px 10px rgba(70, 3, 139, .08);
            direction: rtl;
            font-family: inherit;
        }

        .search-input.active {
            width: 280px;
            opacity: 1;
            border-color: var(--purple);
            box-shadow: 0 4px 20px rgba(70, 3, 139, .15);
        }

        .search-input::placeholder {
            color: #b8a9e0;
            font-weight: 400;
            font-size: 13px;
        }

        .search-close {
            position: absolute;
            left: 12px;
            top: 50%;
            transform: translateY(-50%);
            width: 24px;
            height: 24px;
            border-radius: 50%;
            background: #f0eaff;
            border: none;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 14px;
            color: var(--purple);
            transition: all 0.3s ease;
            opacity: 0;
            visibility: hidden;
            z-index: 3;
        }

        .search-close:hover {
            background: var(--purple);
            color: white;
            transform: translateY(-50%) rotate(90deg);
        }

        .search-close.show {
            opacity: 1;
            visibility: visible;
        }

        /* ===== BREADCRUMB ===== */
        .breadcrumb-bar {
            background: #fff;
            border-bottom: 1px solid var(--border);
            padding: 10px 32px;
            margin-top: 24px;
        }

        .breadcrumb {
            display: flex;
            align-items: center;
            gap: 6px;
            font-size: 12px;
            color: var(--text-mute);
            max-width: 1400px;
            margin: 0 auto;
        }

        .breadcrumb a {
            color: var(--purple);
            text-decoration: none;
            font-weight: 600;
            transition: color var(--transition);
        }

        .breadcrumb a:hover {
            text-decoration: underline;
            color: var(--violet);
        }

        .breadcrumb .sep {
            color: #cbd5e1;
            font-size: 10px;
        }

        .breadcrumb .curr {
            color: var(--text);
            font-weight: 700;
        }

        /* ===== CHECKOUT LAYOUT ===== */
        .checkout-wrap {
            max-width: 1400px;
            margin: 32px auto 80px;
            padding: 0 20px;
            display: grid;
            grid-template-columns: 1fr 360px;
            gap: 30px;
            align-items: flex-start;
        }

        .checkout-wrap.sidebar-hidden {
            grid-template-columns: 1fr;
            max-width: 1000px;
        }

        @media (max-width:1024px) {
            .checkout-wrap {
                grid-template-columns: 1fr;
            }

            .checkout-sidebar {
                position: static !important;
                order: 0;
            }

            .checkout-main {
                order: 1;
            }
        }

        .checkout-sidebar {
            position: sticky;
            top: 20px;
            transition: all 0.4s ease;
        }

        .checkout-sidebar.hidden {
            display: none !important;
        }

        /* ===== STEP HEADER ===== */
        .step-header {
            position: sticky;
            top: 20px;
            z-index: 50;
            background: #fff;
            border-radius: var(--radius-lg);
            border: 1px solid var(--border);
            box-shadow: 0 1px 3px rgba(0, 0, 0, .05);
            padding: 12px 16px;
            margin-bottom: 24px;
        }

        .step-header-inner {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 8px;
        }

        .cstep {
            flex: 1;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 4px;
            cursor: pointer;
            position: relative;
            padding: 6px 0;
            transition: all 0.2s ease;
            border-radius: 8px;
            user-select: none;
        }

        .cstep:hover:not(.disabled) {
            background: var(--lavender);
        }

        .cstep.disabled {
            cursor: not-allowed;
            opacity: 0.5;
        }

        .cstep.disabled:hover {
            background: none;
        }

        .cstep-circle {
            width: 34px;
            height: 34px;
            border-radius: 50%;
            border: 2.5px solid #ddd;
            background: #f8f8f8;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 13px;
            font-weight: 800;
            color: #ccc;
            transition: all 0.3s ease;
        }

        .cstep.active .cstep-circle {
            background: var(--purple);
            border-color: var(--purple);
            color: #fff;
            box-shadow: 0 0 0 6px rgba(70, 3, 139, .12);
        }

        .cstep.done .cstep-circle {
            background: var(--green);
            border-color: var(--green);
            color: #fff;
        }

        .cstep.disabled .cstep-circle {
            background: #f0f0f0;
            border-color: #e0e0e0;
            color: #ccc;
        }

        .cstep-label {
            font-size: 11px;
            font-weight: 600;
            color: var(--text-mute);
            text-align: center;
            white-space: nowrap;
        }

        .cstep.active .cstep-label {
            color: var(--purple);
            font-weight: 700;
        }

        .cstep.done .cstep-label {
            color: var(--green);
            font-weight: 700;
        }

        .cstep:not(:last-child)::after {
            content: '';
            position: absolute;
            top: 22px;
            right: calc(50% + 20px);
            width: calc(100% - 40px);
            height: 2px;
            background: #e2e8f0;
            z-index: 0;
        }

        .cstep.done:not(:last-child)::after {
            background: var(--green);
        }

        .cstep.active:not(:last-child)::after {
            background: var(--purple);
        }

        .cstep.disabled:not(:last-child)::after {
            background: #e8e8e8;
        }

        /* ===== STEP PANELS ===== */
        .step-panel {
            display: none;
            animation: fadeSlide 0.35s ease;
        }

        .step-panel.active {
            display: block;
        }

        @keyframes fadeSlide {
            from {
                opacity: 0;
                transform: translateY(10px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* ===== SECTION CARD ===== */
        .sec-card {
            background: #fff;
            border: 1px solid var(--border);
            border-radius: var(--radius-lg);
            padding: 0;
            margin-bottom: 18px;
            box-shadow: 0 1px 3px rgba(0, 0, 0, .05);
            overflow: hidden;
            width: 100%;
        }

        /* ===== هدر بزرگ مانند تایید نهایی ===== */
        .sec-head {
            background: linear-gradient(120deg, var(--purple) 0%, var(--violet) 60%, var(--navy) 130%);
            padding: 18px 26px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 12px;
            color: #fff;
            position: relative;
            overflow: hidden;
        }

        .sec-head::before {
            content: '';
            position: absolute;
            inset: 0;
            background: radial-gradient(circle at 100% 0%, rgba(255, 255, 255, .12), transparent 55%);
            pointer-events: none;
        }

        .sec-head-left {
            display: flex;
            align-items: center;
            gap: 14px;
            position: relative;
            z-index: 1;
        }

        .sec-ico {
            width: 44px;
            height: 44px;
            border-radius: 12px;
            background: rgba(255, 255, 255, .16);
            backdrop-filter: blur(2px);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            color: #fff;
            border: 1px solid rgba(255, 255, 255, .2);
            flex-shrink: 0;
        }

        .sec-title {
            font-size: 17px;
            font-weight: 800;
            color: #fff;
        }

        .sec-sub {
            font-size: 12px;
            color: rgba(255, 255, 255, .7);
            margin-top: 2px;
        }

        .sec-head-actions {
            display: flex;
            gap: 8px;
            position: relative;
            z-index: 1;
        }

        .sec-body {
            padding: 24px 26px;
        }

        /* ===== FORM ===== */
        .form-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 16px;
        }

        @media (max-width:768px) {
            .form-grid {
                grid-template-columns: 1fr;
            }
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 6px;
        }

        .form-label {
            font-size: 13px;
            font-weight: 700;
            color: var(--text);
        }

        .form-label .req {
            color: var(--danger);
        }

        .form-input {
            width: 100%;
            border: 2px solid var(--border);
            border-radius: 10px;
            padding: 12px 16px;
            font-size: 14px;
            font-family: inherit;
            outline: none;
            background: var(--bg);
            direction: rtl;
            color: var(--text);
            transition: all var(--transition);
        }

        .form-input:focus {
            border-color: var(--purple);
            background: #fff;
            box-shadow: 0 0 0 3px rgba(70, 3, 139, .07);
        }

        .form-input.invalid {
            border-color: var(--danger);
            box-shadow: 0 0 0 3px rgba(220, 38, 38, .06);
        }

        .form-input.valid {
            border-color: var(--green);
        }

        .field-err {
            font-size: 11px;
            color: var(--danger);
            display: none;
            align-items: center;
            gap: 4px;
            margin-top: 2px;
        }

        .field-err.show {
            display: flex;
        }

        .form-input.shake {
            animation: shake .5s ease;
            border-color: var(--danger) !important;
        }

        @keyframes shake {

            0%,
            100% {
                transform: translateX(0)
            }

            20% {
                transform: translateX(-7px)
            }

            40% {
                transform: translateX(7px)
            }

            60% {
                transform: translateX(-4px)
            }

            80% {
                transform: translateX(4px)
            }
        }

        /* ===== BUTTONS ===== */
        .step-next-btn,
        .step-prev-btn {
            background: rgba(255, 255, 255, .2);
            color: #fff;
            border: 1px solid rgba(255, 255, 255, .3);
            border-radius: 8px;
            padding: 8px 18px;
            font-weight: 700;
            font-size: 13px;
            font-family: inherit;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            gap: 6px;
            transition: all 0.2s ease;
        }

        .step-next-btn:hover:not(:disabled),
        .step-prev-btn:hover {
            background: rgba(255, 255, 255, .35);
            transform: translateY(-1px);
        }

        .step-next-btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
            transform: none !important;
        }

        .step-prev-btn {
            background: rgba(255, 255, 255, .1);
        }

        .step-prev-btn:hover {
            background: rgba(255, 255, 255, .25);
        }

        .add-sample-btn {
            background: var(--green);
            color: #fff;
            border: none;
            border-radius: 6px;
            padding: 7px 14px;
            font-weight: 700;
            font-size: 12.5px;
            font-family: inherit;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            gap: 5px;
            transition: all 0.2s ease;
        }

        .add-sample-btn:hover {
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(30, 124, 0, .25);
        }

        .os-pay-btn {
            background: linear-gradient(135deg, var(--green), #16963a);
            color: #fff;
            border: none;
            border-radius: 10px;
            padding: 14px 28px;
            font-size: 15px;
            font-weight: 800;
            font-family: inherit;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            box-shadow: 0 4px 16px rgba(30, 124, 0, .3);
            transition: all 0.2s ease;
        }

        .os-pay-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 28px rgba(30, 124, 0, .4);
        }

        .invoice-final-pay-row {
            margin-top: 18px;
            display: flex;
            justify-content: flex-end;
        }

        .invoice-final-pay-btn {
            width: 100%;
            padding: 18px 28px;
            font-size: 16.5px;
            border-radius: 14px;
            box-shadow: 0 8px 24px rgba(30, 124, 0, .35);
        }

        @media (max-width:640px) {
            .invoice-final-pay-row {
                position: sticky;
                bottom: 0;
                z-index: 5;
                background: #fff;
                padding: 10px 0 4px;
                margin-top: 10px;
            }
        }

        /* ===== INVOICE TABLE (cart) ===== */
        .table-contain {
            overflow: auto;
            border-radius: 12px;
            border: 1px solid var(--border-light);
        }

        .inv-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 13px;
            min-width: 600px;
        }

        .inv-table th,
        .inv-table td {
            text-align: center;
            padding: 12px 10px;
            font-size: 13px;
            vertical-align: middle;
            word-break: break-word;
        }

        .inv-table thead tr {
            background: var(--bg);
        }

        .inv-table th {
            font-weight: 700;
            color: var(--text-mute);
            border-bottom: 2px solid var(--border);
            font-size: 12px;
        }

        .inv-table tbody tr {
            border-bottom: 1px solid var(--border-light);
            transition: background var(--transition);
        }

        .inv-table tbody tr:hover {
            background: #faf9ff;
        }

        .course-img {
            width: 48px;
            height: 34px;
            border-radius: 6px;
            object-fit: cover;
            flex-shrink: 0;
        }

        .course-name {
            font-weight: 700;
            font-size: 13px;
        }

        .course-meta {
            font-size: 11px;
            color: var(--text-mute);
        }

        .price-orig-sm {
            text-decoration: line-through;
            color: #b0b0b0;
            font-size: 12px;
            display: block;
        }

        .price-final-sm {
            font-weight: 900;
            font-size: 14px;
            color: var(--purple);
            display: block;
        }

        .price-final-sm.free {
            color: var(--green);
        }

        .discount-badge {
            display: inline-block;
            background: var(--green-light);
            color: var(--green);
            font-size: 12px;
            font-weight: 700;
            padding: 2px 12px;
            border-radius: 12px;
        }

        .discount-badge.zero {
            background: var(--bg);
            color: var(--text-mute);
        }

        .remove-item-btn {
            background: transparent;
            border: none;
            color: var(--text-mute);
            font-size: 15px;
            cursor: pointer;
            transition: all 0.2s ease;
            padding: 4px 8px;
            border-radius: 4px;
        }

        .remove-item-btn:hover {
            color: var(--danger);
            background: var(--danger-light);
        }

        /* ===== PAYMENT METHOD TABS ===== */
        .method-tabs {
            display: flex;
            border-bottom: 2px solid var(--border);
            margin-bottom: 20px;
            gap: 0;
            flex-wrap: wrap;
        }

        .tab-btn {
            padding: 10px 20px;
            font-weight: 600;
            font-size: 13.5px;
            cursor: pointer;
            border: none;
            background: none;
            color: var(--text-mute);
            font-family: inherit;
            border-bottom: 3px solid transparent;
            transition: all var(--transition);
            position: relative;
            bottom: -2px;
            display: flex;
            align-items: center;
            gap: 7px;
        }

        .tab-btn.active {
            color: var(--purple);
            border-bottom-color: var(--purple);
        }

        .tab-btn:hover:not(.active) {
            color: var(--text);
        }

        .panel {
            display: none;
        }

        .panel.active {
            display: block;
        }

        /* ===== GATEWAY ===== */
        .gateway-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 12px;
        }

        .gw-opt {
            border: 2px solid var(--border);
            border-radius: var(--radius);
            padding: 15px 13px;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 11px;
            position: relative;
            transition: all var(--transition);
            user-select: none;
            background: #fff;
        }

        .gw-opt:hover {
            border-color: var(--purple);
            background: #faf5ff;
            transform: translateY(-2px);
            box-shadow: var(--shadow);
        }

        .gw-opt.chosen {
            border-color: var(--purple);
            background: #f5f0ff;
            box-shadow: 0 0 0 4px rgba(70, 3, 139, .08);
        }

        .gw-opt input[type="radio"] {
            accent-color: var(--purple);
            width: 16px;
            height: 16px;
            flex-shrink: 0;
            cursor: pointer;
        }

        .gw-logo {
            width: 48px;
            height: 34px;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 19px;
            flex-shrink: 0;
        }

        .gw-zp {
            background: #eef2ff;
            color: #4f46e5;
        }

        .gw-wl {
            background: #fdf4ff;
            color: #a21caf;
        }

        .gw-name {
            font-size: 13px;
            font-weight: 700;
            color: var(--text);
        }

        .gw-desc {
            font-size: 10.5px;
            color: var(--text-mute);
            margin-top: 1px;
        }

        /* ===== OFFLINE ===== */
        .bank-info-box {
            background: #f9f8ff;
            border: 1px solid #dcd4fe;
            border-radius: 12px;
            padding: 16px;
            margin-bottom: 14px;
        }

        .bank-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 6px;
            padding: 8px 0;
            border-bottom: 1px dashed #e0e0e0;
            font-size: 13px;
        }

        .bank-row:last-child {
            border-bottom: none;
        }

        .bank-val {
            direction: ltr;
            display: inline-block;
            font-weight: 600;
            letter-spacing: .04em;
        }

        .copy-btn {
            color: var(--purple);
            font-weight: 600;
            cursor: pointer;
            background: none;
            border: none;
            font-family: inherit;
            font-size: 12px;
            padding: 3px 9px;
            border-radius: 6px;
            transition: background var(--transition);
        }

        .copy-btn:hover {
            background: var(--lavender);
        }

        /* ===== DATE / TIME PICKERS ===== */
        .date-picker-wrapper,
        .time-picker-wrapper {
            position: relative;
            width: 100%;
        }

        .date-picker-wrapper .form-input,
        .time-picker-wrapper .time-picker {
            width: 100%;
            padding: 12px 16px 12px 48px;
            font-size: 14px;
            font-family: inherit;
            border: 2px solid var(--border);
            border-radius: 10px;
            background: var(--bg);
            color: var(--text);
            outline: none;
            transition: all 0.25s ease;
        }

        .date-picker-wrapper .form-input:focus,
        .time-picker-wrapper .time-picker:focus {
            background: #fff;
            border-color: var(--purple);
            box-shadow: 0 0 0 3px rgba(70, 3, 139, .07);
        }

        .date-picker-wrapper .form-input.invalid,
        .time-picker-wrapper .time-picker.invalid {
            border-color: var(--danger);
            box-shadow: 0 0 0 3px rgba(220, 38, 38, .06);
        }

        .date-picker-wrapper .form-input.valid,
        .time-picker-wrapper .time-picker.valid {
            border-color: var(--green);
        }

        .date-picker-icon,
        .time-picker-icon {
            position: absolute;
            left: 14px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--purple);
            font-size: 20px;
            pointer-events: auto;
            cursor: pointer;
            opacity: 0.7;
            transition: all 0.25s ease;
            z-index: 2;
        }

        .date-picker-icon:hover,
        .time-picker-icon:hover {
            opacity: 1;
            transform: translateY(-50%) scale(1.1);
        }

        .time-picker {
            direction: ltr;
            text-align: center;
            -moz-appearance: textfield;
        }

        .time-picker::-webkit-outer-spin-button,
        .time-picker::-webkit-inner-spin-button {
            -webkit-appearance: none;
            margin: 0;
        }

        /* ===== PERSIAN DATEPICKER OVERRIDE ===== */
        .persian-datepicker {
            font-family: 'YekanBakhFaNum', sans-serif !important;
            border-radius: 14px !important;
            box-shadow: 0 12px 40px rgba(70, 3, 139, .2) !important;
            border: 1px solid var(--border) !important;
            background: #fff !important;
            overflow: hidden;
            width: 300px !important;
            direction: rtl !important;
            z-index: 1000 !important;
        }

        .persian-datepicker .picker-header {
            background: linear-gradient(135deg, var(--navy), var(--violet)) !important;
            padding: 14px 16px !important;
            border-radius: 0 !important;
            display: flex !important;
            justify-content: space-between !important;
            align-items: center !important;
            direction: rtl !important;
        }

        .persian-datepicker .picker-header .picker-title {
            color: #fff !important;
            font-weight: 700 !important;
            font-size: 15px !important;
        }

        .persian-datepicker .picker-header .picker-arrow {
            color: rgba(255, 255, 255, .8) !important;
            cursor: pointer !important;
            transition: all .2s ease !important;
            font-size: 16px !important;
            padding: 4px 10px !important;
            border-radius: 6px !important;
            background: transparent !important;
            border: none !important;
        }

        .persian-datepicker .picker-header .picker-arrow:hover {
            background: rgba(255, 255, 255, .15) !important;
            color: #fff !important;
        }

        .persian-datepicker .picker-weekdays {
            background: var(--lavender) !important;
            padding: 8px 0 !important;
            display: grid !important;
            grid-template-columns: repeat(7, 1fr) !important;
            text-align: center !important;
        }

        .persian-datepicker .picker-weekdays div {
            color: var(--purple) !important;
            font-weight: 700 !important;
            font-size: 12px !important;
        }

        .persian-datepicker .picker-days {
            padding: 6px !important;
            display: grid !important;
            grid-template-columns: repeat(7, 1fr) !important;
            gap: 3px !important;
            background: #fff !important;
        }

        .persian-datepicker .picker-days .picker-day {
            padding: 6px 4px !important;
            text-align: center !important;
            border-radius: 8px !important;
            cursor: pointer !important;
            transition: all .15s ease !important;
            font-weight: 500 !important;
            font-size: 13px !important;
            color: var(--text) !important;
        }

        .persian-datepicker .picker-days .picker-day:hover {
            background: var(--lavender) !important;
            transform: scale(1.05);
        }

        .persian-datepicker .picker-days .picker-day.today {
            background: rgba(70, 3, 139, .08) !important;
            border: 1.5px solid var(--purple) !important;
            color: var(--purple) !important;
            font-weight: 700 !important;
        }

        .persian-datepicker .picker-days .picker-day.selected {
            background: linear-gradient(135deg, var(--purple), var(--violet)) !important;
            color: #fff !important;
            font-weight: 700 !important;
            box-shadow: 0 4px 12px rgba(70, 3, 139, .3) !important;
        }

        .persian-datepicker .picker-days .picker-day.selected:hover {
            background: var(--violet) !important;
        }

        .persian-datepicker .picker-days .picker-day.other-month {
            color: #d1d5db !important;
        }

        .persian-datepicker .picker-days .picker-day.other-month:hover {
            background: #f3f4f6 !important;
            transform: none !important;
        }

        .persian-datepicker .picker-days .picker-day.holiday {
            color: #ff3000 !important;
        }

        .persian-datepicker .picker-days .picker-day.holiday.selected {
            color: #fff !important;
        }

        .persian-datepicker .picker-footer {
            padding: 8px 14px !important;
            border-top: 1px solid var(--border-light) !important;
            display: flex !important;
            justify-content: space-between !important;
            align-items: center !important;
            background: #fafafa !important;
        }

        .persian-datepicker .picker-footer .picker-today {
            color: var(--purple) !important;
            font-weight: 600 !important;
            font-size: 12px !important;
            cursor: pointer !important;
            transition: color .2s ease !important;
            background: none !important;
            border: none !important;
        }

        .persian-datepicker .picker-footer .picker-today:hover {
            color: var(--violet) !important;
        }

        .persian-datepicker .picker-footer .picker-close {
            color: var(--text-mute) !important;
            font-size: 12px !important;
            cursor: pointer !important;
            transition: color .2s ease !important;
            background: none !important;
            border: none !important;
        }

        .persian-datepicker .picker-footer .picker-close:hover {
            color: var(--danger) !important;
        }

        /* ===== UPLOAD ===== */
        .upload-area {
            border: 2px dashed var(--border);
            border-radius: 12px;
            padding: 22px 16px;
            text-align: center;
            cursor: pointer;
            transition: all var(--transition);
            background: #fafafa;
            margin: 12px 0;
        }

        .upload-area:hover {
            border-color: var(--purple);
            background: #faf5ff;
        }

        .upload-area input {
            display: none;
        }

        .preview-img {
            max-width: 130px;
            border-radius: 8px;
            border: 1px solid var(--border);
            margin-top: 10px;
            display: none;
        }

        .btn-remove-file {
            display: none;
            margin-top: 8px;
            background: var(--danger);
            color: #fff;
            border: none;
            padding: 5px 14px;
            border-radius: 7px;
            cursor: pointer;
            font-family: inherit;
            font-size: 12px;
            font-weight: 600;
        }

        /* ===== COUPON ===== */
        .invoice-coupon {
            margin: 12px 0 16px;
        }

        .coupon-label {
            display: flex;
            align-items: center;
            gap: 7px;
            font-size: 13px;
            font-weight: 700;
            color: var(--text);
            margin-bottom: 10px;
        }

        .coupon-row {
            display: flex;
            gap: 9px;
        }

        .coupon-input {
            flex: 1;
            border: 2px solid var(--border);
            border-radius: 10px;
            padding: 12px 16px;
            font-size: 14px;
            font-family: inherit;
            outline: none;
            background: var(--bg);
            direction: rtl;
            color: var(--text);
            transition: all var(--transition);
        }

        .coupon-input:focus {
            border-color: var(--purple);
            background: #fff;
            box-shadow: 0 0 0 3px rgba(70, 3, 139, .07);
        }

        .coupon-input.shake {
            animation: shake .5s ease;
            border-color: var(--danger) !important;
        }

        .coupon-apply-btn {
            background: var(--purple);
            color: #fff;
            border: none;
            border-radius: 10px;
            padding: 12px 24px;
            font-size: 14px;
            font-weight: 700;
            font-family: inherit;
            cursor: pointer;
            white-space: nowrap;
            display: flex;
            align-items: center;
            gap: 6px;
            transition: all var(--transition);
        }

        .coupon-apply-btn:hover {
            background: var(--violet);
            box-shadow: 0 4px 14px rgba(70, 3, 139, .3);
        }

        .coupon-apply-btn:disabled {
            opacity: .6;
            cursor: not-allowed;
        }

        .coupon-fb {
            display: flex;
            align-items: center;
            gap: 8px;
            border-radius: 9px;
            padding: 10px 14px;
            font-size: 12.5px;
            font-weight: 600;
            margin-top: 10px;
            max-height: 0;
            overflow: hidden;
            opacity: 0;
            transition: all .35s cubic-bezier(.4, 0, .2, 1);
        }

        .coupon-fb.show {
            max-height: 80px;
            opacity: 1;
        }

        .coupon-fb.ok {
            background: var(--green-light);
            border: 1px solid rgba(30, 124, 0, .2);
            color: var(--green);
        }

        .coupon-fb.fail {
            background: var(--danger-light);
            border: 1px solid rgba(220, 38, 38, .2);
            color: var(--danger);
        }

        .cancel-coupon-btn {
            margin-right: 10px;
            background: var(--bg);
            border: 1px solid transparent;
            border-radius: 6px;
            padding: 2px 10px;
            cursor: pointer;
            font-size: 11px;
            font-family: inherit;
            color: #4b5563;
            transition: all .2s ease;
            white-space: nowrap;
        }

        .cancel-coupon-btn:hover {
            background: var(--danger);
            color: white;
        }

        /* ===== TERMS ===== */
        .terms-box {
            background: #fff;
            border: 2px solid var(--border);
            border-radius: var(--radius);
            padding: 15px 18px;
            display: flex;
            align-items: flex-start;
            gap: 10px;
            transition: border-color var(--transition);
        }

        .terms-box input[type="checkbox"] {
            margin-top: 2px;
            accent-color: var(--purple);
            width: 16px;
            height: 16px;
            flex-shrink: 0;
            cursor: pointer;
        }

        .terms-box label {
            font-size: 12.5px;
            color: var(--text-mid);
            line-height: 1.8;
            cursor: pointer;
        }

        .terms-box a {
            color: var(--purple);
            font-weight: 700;
            text-decoration: none;
        }

        .terms-box a:hover {
            text-decoration: underline;
        }

        /* ===== ORDER SIDEBAR ===== */
        .order-summary {
            background: #fff;
            border-radius: var(--radius-xl);
            border: 1px solid var(--border);
            box-shadow: var(--shadow-md);
            overflow: hidden;
        }

        .os-head {
            background: linear-gradient(135deg, var(--navy) 0%, var(--violet) 100%);
            padding: 16px 20px;
            color: #fff;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .os-head-ico {
            width: 38px;
            height: 38px;
            border-radius: 10px;
            background: rgba(255, 255, 255, .14);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 17px;
            flex-shrink: 0;
        }

        .os-head-title {
            font-size: 14px;
            font-weight: 800;
        }

        .os-head-sub {
            font-size: 10px;
            opacity: .65;
            margin-top: 1px;
        }

        .os-items {
            padding: 12px 16px 4px;
            max-height: 200px;
            overflow-y: auto;
        }

        .os-item {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 8px 0;
            border-bottom: 1px solid var(--border-light);
        }

        .os-item:last-child {
            border-bottom: none;
        }

        .os-thumb {
            width: 40px;
            height: 28px;
            border-radius: 6px;
            object-fit: cover;
            border: 1px solid var(--border);
            flex-shrink: 0;
        }

        .os-cname {
            font-size: 11.5px;
            font-weight: 700;
            flex: 1;
            line-height: 1.3;
        }

        .os-cprice {
            font-size: 12px;
            font-weight: 900;
            color: var(--purple);
            white-space: nowrap;
        }

        .os-cprice.free {
            color: var(--green);
        }

        .os-breakdown {
            padding: 8px 16px 0;
        }

        .os-line {
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 12px;
            padding: 3px 0;
        }

        .osl-label {
            color: var(--text-mute);
            font-weight: 500;
        }

        .osl-val {
            font-weight: 600;
            color: var(--text);
        }

        .osl-red {
            font-weight: 700;
            color: var(--danger);
        }

        .os-divider {
            height: 1px;
            background: var(--border-light);
            margin: 6px 0;
        }

        .os-total {
            margin: 8px 16px 0;
            background: var(--lavender);
            border-radius: 12px;
            padding: 12px 14px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border: 1px solid rgba(70, 3, 139, .1);
        }

        .ost-label {
            font-size: 12px;
            font-weight: 800;
            color: var(--violet);
        }

        .ost-sub {
            font-size: 10px;
            color: var(--text-mute);
            margin-top: 1px;
        }

        .ost-amt {
            font-size: 20px;
            font-weight: 900;
            color: var(--purple);
        }

        .ost-unit {
            font-size: 9px;
            color: var(--text-mute);
            text-align: left;
        }

        .os-saving {
            margin: 8px 16px 12px;
            background: var(--green-light);
            border: 1px solid rgba(30, 124, 0, .15);
            border-radius: 8px;
            padding: 7px 12px;
            display: flex;
            align-items: center;
            gap: 6px;
            font-size: 11px;
            font-weight: 700;
            color: var(--green);
        }

        /* ===== FINAL INVOICE (مرحله ۴) ===== */
        .final-invoice-wrapper {
            display: flex;
            justify-content: center;
            align-items: flex-start;
            width: 100%;
        }

        .final-invoice {
            background: #fff;
            border-radius: var(--radius-xl);
            border: 1px solid var(--border-light);
            box-shadow: var(--shadow-lg);
            padding: 0 0 28px;
            max-width: 100%;
            width: 100%;
            overflow: hidden;
            animation: fadeSlide .4s ease;
        }

        /* هدر ساده فاکتور نهایی - فقط "جزئیات خرید شما" چسبیده به راست */
        .invoice-final-header {
            background: linear-gradient(120deg, var(--purple) 0%, var(--violet) 60%, var(--navy) 130%);
            padding: 18px 32px;
            position: relative;
            overflow: hidden;
            display: flex;
            justify-content: flex-start;
            align-items: center;
        }

        .invoice-final-header::before {
            content: '';
            position: absolute;
            inset: 0;
            background: radial-gradient(circle at 100% 0%, rgba(255, 255, 255, .12), transparent 55%);
            pointer-events: none;
        }

        .invoice-final-header-content {
            display: flex;
            align-items: center;
            gap: 14px;
            position: relative;
            z-index: 1;
        }

        .invoice-final-header-content .invoice-icon {
            width: 44px;
            height: 44px;
            border-radius: 12px;
            background: rgba(255, 255, 255, .16);
            backdrop-filter: blur(2px);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            color: #fff;
            border: 1px solid rgba(255, 255, 255, .2);
        }

        .invoice-final-header-content h2 {
            font-size: 18px;
            font-weight: 800;
            color: #fff;
            margin: 0;
        }

        .invoice-final-header-content .invoice-number {
            font-size: 12px;
            color: rgba(255, 255, 255, .7);
            font-weight: 600;
            display: block;
            margin-top: 2px;
        }

        .invoice-info-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 16px;
            margin: 20px 32px;
        }

        @media (max-width:768px) {
            .invoice-info-grid {
                grid-template-columns: 1fr;
                margin: 16px 18px;
            }
        }

        .invoice-info-box {
            background: var(--bg);
            border-radius: var(--radius-lg);
            padding: 16px 20px;
            border: 1px solid var(--border-light);
            transition: var(--transition);
        }

        .invoice-info-box:hover {
            border-color: var(--lavender);
            box-shadow: 0 4px 16px rgba(70, 3, 139, .06);
        }

        .invoice-info-title {
            font-size: 12px;
            font-weight: 800;
            color: var(--purple);
            margin-bottom: 10px;
            display: flex;
            align-items: center;
            gap: 7px;
        }

        .invoice-info-title i {
            width: 24px;
            height: 24px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            background: var(--lavender);
            border-radius: 8px;
            font-size: 11px;
        }

        .invoice-info-body {
            font-size: 13px;
            color: var(--text);
            line-height: 2;
        }

        .invoice-info-body div {
            display: flex;
            align-items: center;
            padding: 2px 0;
        }

        .invoice-info-body div span:first-child {
            color: var(--text-mute);
            font-weight: 500;
        }

        .invoice-info-body div span:last-child {
            font-weight: 700;
            margin-right: 10px;
        }

        .invoice-table-wrap {
            margin: 0 32px 20px;
            overflow: auto;
            border-radius: var(--radius-lg);
            border: 1px solid var(--border-light);
            box-shadow: 0 2px 10px rgba(15, 23, 42, .03);
        }

        @media (max-width:768px) {
            .invoice-table-wrap {
                margin: 0 18px 20px;
            }
        }

        .invoice-items-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 13px;
            min-width: 500px;
        }

        .invoice-items-table th {
            background: linear-gradient(135deg, var(--lavender), #f3edff);
            padding: 12px 14px;
            font-size: 12px;
            font-weight: 800;
            color: var(--purple);
            text-align: center;
            border-bottom: 2px solid var(--border-light);
        }

        .invoice-items-table td {
            padding: 12px 14px;
            text-align: center;
            border-bottom: 1px solid var(--border-light);
            vertical-align: middle;
        }

        .invoice-items-table tbody tr {
            transition: background var(--transition);
        }

        .invoice-items-table tbody tr:hover {
            background: #faf9ff;
        }

        .invoice-items-table tr:last-child td {
            border-bottom: none;
        }

        .invoice-items-table .inv-price-orig {
            text-decoration: line-through;
            color: #b0b0b0;
            font-size: 12px;
        }

        .invoice-items-table .inv-price-final {
            font-weight: 900;
            font-size: 14px;
            color: var(--purple);
        }

        .invoice-items-table .inv-price-final.free {
            color: var(--green);
        }

        .invoice-summary {
            background: linear-gradient(135deg, #fbfaff, var(--bg));
            border-radius: var(--radius-lg);
            padding: 18px 22px;
            margin: 0 32px 12px;
            border: 1px solid var(--border-light);
        }

        @media (max-width:768px) {
            .invoice-summary {
                margin: 0 18px 12px;
            }
        }

        .invoice-summary-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 5px 0;
            font-size: 13px;
        }

        .invoice-summary-row.discount {
            color: var(--danger);
            font-weight: 600;
        }

        .invoice-summary-row.grand-total {
            font-size: 18px;
            font-weight: 900;
            color: var(--purple);
            padding: 12px 16px;
            margin-top: 6px;
            background: var(--lavender);
            border-radius: var(--radius);
        }

        .invoice-summary-divider {
            height: 1px;
            background: var(--border-light);
            margin: 6px 0;
        }

        .invoice-summary-words {
            font-size: 11px;
            color: var(--text-mute);
            text-align: left;
            margin-top: 4px;
        }

        .invoice-footer-block {
            padding: 0 32px;
        }

        @media (max-width:768px) {
            .invoice-footer-block {
                padding: 0 18px;
            }
        }

        /* ===== MODAL ===== */
        .modal-ov {
            position: fixed;
            inset: 0;
            background: rgba(15, 23, 42, .62);
            z-index: 9999;
            display: none;
            align-items: center;
            justify-content: center;
            padding: 20px;
            backdrop-filter: blur(6px);
        }

        .modal-ov.on {
            display: flex;
        }

        .modal-box {
            background: #fff;
            border-radius: 22px;
            padding: 38px 34px;
            max-width: 440px;
            width: 100%;
            text-align: center;
            box-shadow: 0 30px 80px rgba(0, 0, 0, .2);
            animation: popIn .4s cubic-bezier(.17, .67, .35, 1.25);
        }

        @keyframes popIn {
            from {
                transform: scale(.8);
                opacity: 0;
            }

            to {
                transform: scale(1);
                opacity: 1;
            }
        }

        .modal-circle {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            background: var(--green-light);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 36px;
            margin: 0 auto 18px;
            color: var(--green);
            animation: bounceIn .6s .2s both;
        }

        @keyframes bounceIn {
            0% {
                transform: scale(0);
            }

            55% {
                transform: scale(1.2);
            }

            100% {
                transform: scale(1);
            }
        }

        .modal-box h2 {
            font-size: 21px;
            font-weight: 900;
            color: var(--green);
            margin-bottom: 8px;
        }

        .modal-box p {
            font-size: 13.5px;
            color: var(--text-mid);
            line-height: 1.7;
            margin-bottom: 18px;
        }

        .track-code-box {
            background: var(--bg);
            border: 1.5px dashed #cbd5e1;
            border-radius: 12px;
            padding: 14px;
            margin-bottom: 20px;
        }

        .track-code-label {
            font-size: 11.5px;
            color: var(--text-mute);
        }

        .track-code-val {
            font-size: 20px;
            font-weight: 900;
            color: var(--purple);
            letter-spacing: 1.5px;
            margin-top: 4px;
            display: block;
        }

        .modal-actions {
            display: flex;
            gap: 10px;
        }

        .modal-cta-primary {
            flex: 1;
            background: linear-gradient(135deg, var(--navy), var(--violet));
            color: #fff;
            border: none;
            border-radius: 11px;
            padding: 13px;
            font-size: 14px;
            font-weight: 800;
            font-family: inherit;
            cursor: pointer;
            transition: all var(--transition);
        }

        .modal-cta-primary:hover {
            opacity: .9;
            transform: translateY(-1px);
        }

        .modal-cta-sec {
            background: var(--bg);
            color: var(--text-mid);
            border: 2px solid var(--border);
            border-radius: 11px;
            padding: 13px 18px;
            font-size: 13.5px;
            font-weight: 700;
            font-family: inherit;
            cursor: pointer;
            transition: all var(--transition);
        }

        .modal-cta-sec:hover {
            background: #e2e8f0;
        }

        .modal-note {
            margin-top: 12px;
            font-size: 11px;
            color: var(--text-mute);
        }

        /* ===== TOAST ===== */
        .toast-container {
            position: fixed;
            bottom: 28px;
            left: 50%;
            transform: translateX(-50%);
            z-index: 99999;
            display: flex;
            flex-direction: column;
            gap: 8px;
            align-items: center;
            pointer-events: none;
        }

        .toast {
            background: #1e293b;
            color: #fff;
            padding: 12px 22px;
            border-radius: 12px;
            font-size: 13px;
            font-weight: 700;
            font-family: inherit;
            box-shadow: 0 8px 28px rgba(0, 0, 0, .22);
            display: flex;
            align-items: center;
            gap: 8px;
            transform: translateY(50px);
            opacity: 0;
            transition: transform .35s cubic-bezier(.17, .67, .35, 1.2), opacity .3s;
            pointer-events: auto;
            max-width: 90vw;
            white-space: nowrap;
        }

        .toast.show {
            transform: translateY(0);
            opacity: 1;
        }

        .toast.t-error {
            background: #991b1b;
        }

        .toast.t-success {
            background: #065f46;
        }

        /* ===== FOOTER ===== */
        .footer {
            position: relative;
            overflow: hidden;
            margin-top: 80px;
            padding: 70px 50px;
            border-radius: 70px 70px 0 0;
            background: linear-gradient(135deg, #0d0d1e 0%, #1a0a2e 100%);
            min-height: 400px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .footer::before {
            content: '';
            position: absolute;
            inset: 0;
            background: url('data:image/svg+xml;base64,PHN2ZyB4bWxucz0iaHR0cDovL3d3dy53My5vcmcvMjAwMC9zdmciIHdpZHRoPSI2MCIgaGVpZ2h0PSI2MCIgdmlld0JveD0iMCAwIDYwIDYwIj48cGF0aCBkPSJNMzAgMTBsMTAgMjBIMjB6IiBmaWxsPSJyZ2JhKDI1NSwyNTUsMjU1LDAuMDMpIi8+PC9zdmc+') repeat;
            opacity: .3;
            pointer-events: none;
        }

        .footer-overlay {
            position: absolute;
            inset: 0;
            background: rgba(0, 0, 0, .75);
        }

        .footer-content {
            position: relative;
            z-index: 2;
            display: flex;
            justify-content: space-between;
            gap: 40px;
            align-items: flex-start;
            width: 100%;
            max-width: 1400px;
            flex-wrap: wrap;
        }

        .footer-col {
            flex: 1;
            min-width: 180px;
        }

        .footer-col h3,
        .footer-col h2 {
            color: var(--cyan);
            margin-bottom: 25px;
            font-size: 16px;
            font-weight: 800;
        }

        .footer-col h2 {
            font-size: 22px;
        }

        .footer-col p {
            color: rgba(255, 255, 255, .6);
            line-height: 1.9;
            margin-bottom: 25px;
            font-size: 13px;
        }

        .footer-col p a {
            text-decoration: none;
            color: rgba(255, 255, 255, .8);
            transition: color var(--transition);
            font-weight: 500;
        }

        .footer-col p a:hover {
            color: var(--cyan);
        }

        .footer-col ul {
            list-style: none;
            padding-right: 0;
        }

        .footer-col ul li {
            color: rgba(255, 255, 255, .5);
            margin-bottom: 12px;
            transition: all .3s ease;
            cursor: pointer;
            text-align: right;
            font-size: 13px;
        }

        .footer-col ul li:hover {
            color: #fff;
            transform: translateX(-6px);
        }

        .footer-col ul li a {
            color: rgba(255, 255, 255, .5);
            text-decoration: none;
            transition: .3s;
        }

        .footer-col ul li a:hover {
            color: #fff;
        }

        .footer-col.center {
            text-align: center;
        }

        .footer-col:first-child {
            text-align: right;
        }

        .footer-col:last-child {
            text-align: left;
        }

        .socials {
            display: flex;
            justify-content: center;
            gap: 18px;
            margin-top: 25px;
        }

        .socials a {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            background: rgba(255, 255, 255, .06);
            border: 1px solid rgba(255, 255, 255, .08);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 22px;
            color: rgba(255, 255, 255, .5);
            transition: all .3s ease;
            text-decoration: none;
        }

        .socials a:hover {
            background: var(--purple);
            border-color: var(--purple);
            color: #fff;
            transform: translateY(-4px);
            box-shadow: 0 8px 24px rgba(70, 3, 139, .4);
        }

        /* ===== RESPONSIVE ===== */
        @media (max-width:1024px) {
            .footer-content {
                gap: 30px;
                flex-wrap: wrap;
            }

            .footer-col {
                flex: 0 0 calc(50% - 15px);
                min-width: auto;
            }

            .footer-col:first-child {
                flex: 0 0 100%;
                text-align: center !important;
            }

            .footer-col:last-child {
                text-align: center !important;
            }

            .footer {
                padding: 40px 24px;
                border-radius: 40px 40px 0 0;
                min-height: auto;
            }
        }

        @media (max-width:768px) {
            .navbar {
                flex-direction: column;
                align-items: center;
                gap: 15px;
                padding: 10px;
                margin-top: 10px;
            }

            .nav-menu {
                display: flex;
                flex-wrap: wrap;
                justify-content: center;
                gap: 8px;
                width: 100%;
            }

            .nav-menu a {
                font-size: 13px;
                padding: 6px 12px;
            }

            .header-left {
                display: flex;
                flex-wrap: wrap;
                justify-content: center;
                gap: 12px;
                width: 100%;
            }

            .login-box {
                width: 130px;
                height: 38px;
                font-size: 12px;
                gap: 8px;
            }

            .login-box span {
                margin-right: 8px;
            }

            .arrow-circle {
                width: 34px;
                height: 34px;
                right: -6px;
            }

            .arrow-circle .circle {
                width: 31px;
                height: 31px;
            }

            .arrow-circle .circle i {
                font-size: 16px;
                transform: translateX(4px);
            }

            .cart-box {
                width: 38px;
                height: 38px;
                margin-left: 8px;
                font-size: 18px;
            }

            .search-input.active {
                width: 180px;
            }

            .search-input {
                padding: 10px 35px 10px 12px;
                font-size: 13px;
            }

            .search-close {
                width: 22px;
                height: 22px;
                font-size: 12px;
                left: 8px;
            }

            .checkout-wrap {
                padding: 0 12px;
            }

            .gateway-grid {
                grid-template-columns: 1fr;
            }

            .form-grid {
                grid-template-columns: 1fr;
            }

            .modal-box {
                padding: 26px 16px;
            }

            .modal-actions {
                flex-direction: column;
            }

            .step-header-inner {
                flex-wrap: wrap;
                justify-content: center;
                gap: 4px;
            }

            .cstep {
                flex: 0 0 auto;
                padding: 4px 8px;
            }

            .cstep:not(:last-child)::after {
                display: none;
            }

            .cstep-circle {
                width: 28px;
                height: 28px;
                font-size: 11px;
            }

            .cstep-label {
                font-size: 9px;
            }

            .inv-table th,
            .inv-table td {
                font-size: 12px;
                padding: 8px 6px;
            }

            .invoice-final-header {
                padding: 14px 18px;
            }

            .invoice-final-header-content h2 {
                font-size: 16px;
            }

            .invoice-final-header-content .invoice-icon {
                width: 38px;
                height: 38px;
                font-size: 17px;
            }

            .invoice-info-grid {
                margin: 14px 18px;
            }

            .invoice-table-wrap {
                margin: 0 18px 14px;
            }

            .invoice-summary {
                margin: 0 18px 12px;
                padding: 14px 16px;
            }

            .invoice-footer-block {
                padding: 0 18px;
            }

            .sec-head {
                padding: 14px 18px;
            }

            .sec-title {
                font-size: 15px;
            }

            .sec-body {
                padding: 18px 18px;
            }

            .date-picker-wrapper .form-input,
            .time-picker-wrapper .time-picker {
                font-size: 13px;
                padding: 10px 12px 10px 44px;
            }

            .date-picker-icon,
            .time-picker-icon {
                font-size: 18px;
                left: 12px;
            }

            .persian-datepicker {
                width: 250px !important;
                font-size: 12px !important;
            }

            .persian-datepicker .picker-days .picker-day {
                font-size: 11px !important;
                padding: 4px 2px !important;
            }
        }

        @media (max-width:400px) {
            .method-tabs {
                flex-wrap: wrap;
            }

            .tab-btn {
                font-size: 12px;
                padding: 8px 12px;
            }

            .os-pay-btn {
                font-size: 14px;
                padding: 14px;
            }

            .persian-datepicker {
                width: 210px !important;
                font-size: 11px !important;
            }
        }
    </style>
</head>

<body>

    <!-- HEADER -->
    <header>
        <nav class="navbar">
            <ul class="nav-menu">
                <li><a href="#" class="active" onclick="showToast('صفحه اصلی', 'info'); return false;">صفحه اصلی</a>
                </li>
                <li><a href="#" onclick="showToast('دوره‌های تخصصی', 'info'); return false;">دوره‌های تخصصی</a></li>
                <li><a href="#" onclick="showToast('فایل لایه باز', 'info'); return false;">فایل لایه باز</a></li>
                <li><a href="#" onclick="showToast('جعبه ابزار', 'info'); return false;">جعبه ابزار</a></li>
                <li><a href="#" onclick="showToast('تماس با ما', 'info'); return false;">تماس با ما</a></li>
            </ul>
            <div class="header-left">
                <div class="search-container">
                    <button class="search-icon" id="searchToggle"><i class="fa-solid fa-magnifying-glass"></i></button>
                    <div class="search-input-wrapper">
                        <input type="text" class="search-input" id="searchInput" placeholder="جستجو کنید..." />
                        <button class="search-close" id="searchClose">✕</button>
                    </div>
                </div>
                <button class="cart-box" onclick="showToast('سبد خرید شما خالی است', 'info');">
                    <i class="fa-solid fa-cart-shopping"></i>
                    <span class="badge">۴</span>
                </button>
                <button class="login-box" onclick="showToast('صفحه ورود / ثبت نام', 'info');">
                    <div class="arrow-circle">
                        <div class="circle"><i class="fa-solid fa-arrow-left"></i></div>
                    </div>
                    <span>ورود / ثبت نام</span>
                </button>
            </div>
        </nav>
    </header>

    <!-- BREADCRUMB -->
    <div class="breadcrumb-bar">
        <div class="breadcrumb">
            <a href="#" onclick="showToast('خانه', 'info'); return false;"><i class="fa-solid fa-house"></i> خانه</a>
            <span class="sep"><i class="fa-solid fa-chevron-left"></i></span>
            <a href="#" onclick="showToast('دوره‌ها', 'info'); return false;">دوره‌ها</a>
            <span class="sep"><i class="fa-solid fa-chevron-left"></i></span>
            <span class="curr">پرداخت</span>
        </div>
    </div>

    <!-- CHECKOUT WRAP -->
    <div class="checkout-wrap" id="checkoutWrap">
        <main class="checkout-main" id="checkoutMain">

            <!-- STEP HEADER -->
            <div class="step-header" id="stepHeader">
                <div class="step-header-inner">
                    <div class="cstep active" data-step="1">
                        <div class="cstep-circle"><i class="fa-solid fa-user"></i></div>
                        <div class="cstep-label">اطلاعات کاربر</div>
                    </div>
                    <div class="cstep disabled" data-step="2">
                        <div class="cstep-circle"><i class="fa-solid fa-cart-shopping"></i></div>
                        <div class="cstep-label">سبد خرید</div>
                    </div>
                    <div class="cstep disabled" data-step="3">
                        <div class="cstep-circle"><i class="fa-solid fa-credit-card"></i></div>
                        <div class="cstep-label">پرداخت</div>
                    </div>
                    <div class="cstep disabled" data-step="4">
                        <div class="cstep-circle"><i class="fa-solid fa-circle-check"></i></div>
                        <div class="cstep-label">تأیید نهایی</div>
                    </div>
                </div>
            </div>

            <!-- STEP 1: اطلاعات کاربر -->
            <div class="step-panel active" data-step="1">
                <div class="sec-card">
                    <div class="sec-head">
                        <div class="sec-head-left">
                            <div class="sec-ico"><i class="fa-solid fa-user-pen"></i></div>
                            <div>
                                <div class="sec-title">اطلاعات خریدار</div>
                                <div class="sec-sub">لطفاً مشخصات خود را وارد کنید</div>
                            </div>
                        </div>
                        <div class="sec-head-actions">
                            <button class="step-prev-btn" data-prev="1" disabled style="opacity:0.4;cursor:default;"><i
                                    class="fa-solid fa-chevron-right"></i> قبلی</button>
                            <button class="step-next-btn" id="step1NextBtn" data-next="2" disabled>بعدی <i
                                    class="fa-solid fa-chevron-left"></i></button>
                        </div>
                    </div>
                    <div class="sec-body">
                        <div class="form-grid">
                            <div class="form-group">
                                <label class="form-label">نام <span class="req">*</span></label>
                                <input type="text" class="form-input" id="fname" placeholder="نام خود را وارد کنید" />
                                <span class="field-err" id="fname-err"><i class="fa-solid fa-triangle-exclamation"></i>
                                    لطفاً نام را وارد کنید</span>
                            </div>
                            <div class="form-group">
                                <label class="form-label">نام خانوادگی <span class="req">*</span></label>
                                <input type="text" class="form-input" id="lname"
                                    placeholder="نام خانوادگی خود را وارد کنید" />
                                <span class="field-err" id="lname-err"><i class="fa-solid fa-triangle-exclamation"></i>
                                    لطفاً نام خانوادگی را وارد کنید</span>
                            </div>
                            <div class="form-group">
                                <label class="form-label">شماره موبایل <span class="req">*</span></label>
                                <input type="tel" class="form-input" id="phone" placeholder="۰۹xxxxxxxxx"
                                    style="direction:ltr;text-align:right" />
                                <span class="field-err" id="phone-err"><i class="fa-solid fa-triangle-exclamation"></i>
                                    شماره موبایل معتبر نیست</span>
                            </div>
                            <div class="form-group">
                                <label class="form-label">آدرس ایمیل</label>
                                <input type="email" class="form-input" id="email" placeholder="example@email.com"
                                    style="direction:ltr;text-align:right" />
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- STEP 2: سبد خرید -->
            <div class="step-panel" data-step="2">
                <div class="sec-card">
                    <div class="sec-head">
                        <div class="sec-head-left">
                            <div class="sec-ico"><i class="fa-solid fa-cart-shopping"></i></div>
                            <div>
                                <div class="sec-title">سبد خرید</div>
                                <div class="sec-sub">محصولات خود را مرور کنید</div>
                            </div>
                        </div>
                        <div class="sec-head-actions">
                            <button class="step-prev-btn" data-prev="1"><i class="fa-solid fa-chevron-right"></i>
                                قبلی</button>
                            <button class="step-next-btn" data-next="3">بعدی <i
                                    class="fa-solid fa-chevron-left"></i></button>
                        </div>
                    </div>
                    <div class="sec-body">
                        <div class="table-contain">
                            <table class="inv-table" id="cartTable">
                                <thead>
                                    <tr>
                                        <th style="width:28%;">دوره آموزشی</th>
                                        <th style="width:22%;">قیمت اصلی</th>
                                        <th style="width:18%;">تخفیف</th>
                                        <th style="width:22%;">قیمت نهایی</th>
                                        <th style="width:10%;">عملیات</th>
                                    </tr>
                                </thead>
                                <tbody id="cartBody"></tbody>
                            </table>
                        </div>
                        <div
                            style="display:flex; justify-content:space-between; align-items:center; margin-top:16px; padding:12px 0; border-top:1px solid var(--border);">
                            <div>
                                <span style="font-weight:700;font-size:14px;">جمع کل: </span>
                                <span id="cartTotal"
                                    style="font-weight:900;font-size:18px;color:var(--purple);">۰</span>
                                <span style="font-size:12px;color:var(--text-mute);"> تومان</span>
                            </div>
                            <button class="add-sample-btn" id="addSampleBtn"><i class="fa-solid fa-plus"></i> افزودن
                                نمونه</button>
                        </div>
                        <div class="invoice-coupon">
                            <div class="coupon-label"><i class="fa-solid fa-ticket" style="color:var(--purple);"></i> کد
                                تخفیف دارید؟</div>
                            <div class="coupon-row">
                                <input type="text" class="coupon-input" id="couponInput"
                                    placeholder="کد تخفیف را وارد کنید" autocomplete="off" />
                                <button class="coupon-apply-btn" id="couponBtn"><span id="couponBtnTxt"><i
                                            class="fa-solid fa-check-circle"></i> اعمال</span></button>
                            </div>
                            <div class="coupon-fb ok" id="couponOk"></div>
                            <div class="coupon-fb fail" id="couponErr"><i class="fa-solid fa-circle-exclamation"></i> کد
                                وارد‌شده معتبر نیست.</div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- STEP 3: پرداخت -->
            <div class="step-panel" data-step="3">
                <div class="sec-card">
                    <div class="sec-head">
                        <div class="sec-head-left">
                            <div class="sec-ico"><i class="fa-solid fa-shield-halved"></i></div>
                            <div>
                                <div class="sec-title">روش پرداخت</div>
                                <div class="sec-sub">درگاه یا روش پرداخت مناسب خود را انتخاب کنید</div>
                            </div>
                        </div>
                        <div class="sec-head-actions">
                            <button class="step-prev-btn" data-prev="2"><i class="fa-solid fa-chevron-right"></i>
                                قبلی</button>
                            <button class="step-next-btn" data-next="4">بعدی <i
                                    class="fa-solid fa-chevron-left"></i></button>
                        </div>
                    </div>
                    <div class="sec-body">
                        <div class="method-tabs" id="methodTabs">
                            <button class="tab-btn active" data-method="offline"><i
                                    class="fa-solid fa-building-columns"></i> کارت به کارت</button>
                            <button class="tab-btn" data-method="online"><i class="fa-solid fa-credit-card"></i> پرداخت
                                آنلاین</button>
                        </div>
                        <div class="panel" id="panel-online">
                            <p style="font-size:13px;color:var(--text-mute);margin-bottom:14px;">درگاه بانکی مورد نظر
                                خود را انتخاب کنید:</p>
                            <div class="gateway-grid" id="gatewayGrid">
                                <label class="gw-opt chosen" data-gw="zarinpal">
                                    <input type="radio" name="gw" value="zarinpal" checked />
                                    <div class="gw-logo gw-zp"><i class="fa-solid fa-building-columns"></i></div>
                                    <div>
                                        <div class="gw-name">زرین‌پال</div>
                                        <div class="gw-desc">پشتیبانی تمام بانک‌ها</div>
                                    </div>
                                </label>
                                <label class="gw-opt" data-gw="wallet">
                                    <input type="radio" name="gw" value="wallet" />
                                    <div class="gw-logo gw-wl"><i class="fa-solid fa-wallet"></i></div>
                                    <div>
                                        <div class="gw-name">کیف پول</div>
                                        <div class="gw-desc">موجودی: ۱۵۰,۰۰۰ تومان</div>
                                    </div>
                                </label>
                            </div>
                        </div>
                        <div class="panel active" id="panel-offline">
                            <div class="bank-info-box">
                                <div class="bank-row"><span><i class="fa-solid fa-building-columns"
                                            style="color:var(--purple);width:18px;"></i> بانک</span><strong>بانک ملی
                                        ایران</strong></div>
                                <div class="bank-row"><span><i class="fa-solid fa-user"
                                            style="color:var(--purple);width:18px;"></i> صاحب
                                        حساب</span><strong>آموزشگاه پیشتاز</strong></div>
                                <div class="bank-row">
                                    <span><i class="fa-solid fa-credit-card"
                                            style="color:var(--purple);width:18px;"></i> شماره کارت</span>
                                    <div style="display:flex;align-items:center;gap:8px;">
                                        <span class="bank-val">6037-9918-0000-1234</span>
                                        <button class="copy-btn" onclick="copyText('6037991800001234','شماره کارت')"><i
                                                class="fa-solid fa-copy"></i> کپی</button>
                                    </div>
                                </div>
                                <div class="bank-row">
                                    <span><i class="fa-solid fa-hashtag" style="color:var(--purple);width:18px;"></i>
                                        شبا</span>
                                    <div style="display:flex;align-items:center;gap:8px;">
                                        <span class="bank-val"
                                            style="font-size:11.5px;">IR950170000000123456789003</span>
                                        <button class="copy-btn"
                                            onclick="copyText('IR950170000000123456789003','شبا')"><i
                                                class="fa-solid fa-copy"></i> کپی</button>
                                    </div>
                                </div>
                            </div>
                            <p style="font-size:13px;color:var(--text-mid);margin-bottom:12px;font-weight:500;">پس از
                                واریز، اطلاعات زیر را تکمیل کنید:</p>
                            <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-bottom:12px;">
                                <div class="form-group">
                                    <label class="form-label" style="font-size:12px;"><i
                                            class="fa-regular fa-calendar"></i> تاریخ واریز <span
                                            class="req">*</span></label>
                                    <div class="date-picker-wrapper">
                                        <input type="text" class="form-input" id="offDate"
                                            placeholder="انتخاب یا وارد کنید" autocomplete="off" />
                                        <i class="fa-regular fa-calendar-alt date-picker-icon"></i>
                                    </div>
                                    <span class="field-err" id="offDate-err"><i
                                            class="fa-solid fa-triangle-exclamation"></i> لطفاً تاریخ واریز را انتخاب یا
                                        وارد کنید</span>
                                </div>
                                <div class="form-group">
                                    <label class="form-label" style="font-size:12px;"><i
                                            class="fa-regular fa-clock"></i> ساعت واریز <span
                                            class="req">*</span></label>
                                    <div class="time-picker-wrapper">
                                        <input type="text" class="form-input time-picker" id="offTime"
                                            placeholder="انتخاب یا وارد کنید" autocomplete="off" />
                                        <i class="fa-regular fa-clock time-picker-icon"></i>
                                    </div>
                                    <span class="field-err" id="offTime-err"><i
                                            class="fa-solid fa-triangle-exclamation"></i> لطفاً ساعت واریز را انتخاب یا
                                        وارد کنید</span>
                                </div>
                            </div>
                            <div class="form-group" style="margin-bottom:10px;">
                                <label class="form-label" style="font-size:12px;">کد پیگیری / شماره تراکنش <span
                                        style="font-size:10px;color:var(--text-mute);font-weight:400;">(اختیاری)</span></label>
                                <input type="text" class="form-input" id="offRef"
                                    placeholder="در صورت تمایل وارد کنید" />
                            </div>
                            <div class="upload-area" id="uploadBox">
                                <div style="font-size:28px;margin-bottom:4px;color:var(--purple);"><i
                                        class="fa-regular fa-image"></i></div>
                                <div style="font-weight:600;font-size:13px;">برای بارگذاری فیش واریزی کلیک کنید <span
                                        class="req">*</span></div>
                                <div style="font-size:11px;color:var(--text-mute);margin-top:2px;">فرمت‌های مجاز: jpg،
                                    png، pdf</div>
                                <input type="file" id="receiptFile" accept="image/*,.pdf" />
                            </div>
                            <span class="field-err" id="receiptFile-err" style="display:none;margin-top:4px;"><i
                                    class="fa-solid fa-triangle-exclamation"></i> لطفاً فیش واریزی را بارگذاری
                                کنید</span>
                            <img id="previewImg" class="preview-img" src="" alt="پیش‌نمایش فیش" />
                            <button type="button" id="removeFileBtn" class="btn-remove-file"><i
                                    class="fa-solid fa-trash"></i> حذف فایل</button>
                        </div>
                    </div>
                </div>
            </div>

            <!-- STEP 4: تأیید نهایی -->
            <div class="step-panel" data-step="4">
                <div class="final-invoice-wrapper">
                    <div class="final-invoice">
                        <!-- هدر ساده فقط "جزئیات خرید شما" چسبیده به راست -->
                        <div class="invoice-final-header">
                            <div class="invoice-final-header-content">
                                <div class="invoice-icon"><i class="fa-solid fa-file-invoice"></i></div>
                                <div>
                                    <h2>جزئیات خرید شما</h2>
                                    <span class="invoice-number">#<span id="invoiceNumber">PTZ-1404-0087</span></span>
                                </div>
                            </div>
                        </div>

                        <div class="invoice-info-grid">
                            <div class="invoice-info-box">
                                <div class="invoice-info-title"><i class="fa-solid fa-user"></i> اطلاعات خریدار</div>
                                <div style="display:flex; justify-content:space-between; gap:12px;">
                                    <div class="invoice-info-body" style="flex:1;">
                                        <div><span>نام:</span> <span id="finalFname">—</span></div>
                                        <div><span>نام خانوادگی:</span> <span id="finalLname">—</span></div>
                                    </div>
                                    <div class="invoice-info-body" style="flex:1;">
                                        <div><span>موبایل:</span> <span id="finalPhone">—</span></div>
                                        <div><span>ایمیل:</span> <span id="finalEmail">—</span></div>
                                    </div>
                                </div>
                            </div>
                            <div class="invoice-info-box">
                                <div class="invoice-info-title"><i class="fa-solid fa-credit-card"></i> روش پرداخت</div>
                                <div style="flex:1;">
                                    <div id="finalReceiptInfo"
                                        style="display:grid; grid-template-columns:auto auto; gap:2px 16px;"></div>
                                </div>
                            </div>
                        </div>

                        <div class="invoice-table-wrap">
                            <table class="invoice-items-table">
                                <thead>
                                    <tr>
                                        <th style="width:40%;">جزئیات دوره‌های خریداری‌شده</th>
                                        <th style="width:30%;">قیمت اصلی</th>
                                        <th style="width:30%;">قیمت نهایی</th>
                                    </tr>
                                </thead>
                                <tbody id="finalInvoiceItems"></tbody>
                            </table>
                        </div>

                        <div class="invoice-summary">
                            <div class="invoice-summary-row"><span>جمع قیمت اصلی دوره‌ها</span><span
                                    id="invoiceOrigTotal">۰ ت</span></div>
                            <div class="invoice-summary-row" id="invoiceCouponRow" style="display:none;"><span><i
                                        class="fa-solid fa-ticket"></i> تخفیف کد</span><span class="text-red"
                                    id="invoiceCouponDisc">—</span></div>
                            <div class="invoice-summary-row discount"><span>تخفیف کل دوره‌ها</span><span
                                    class="text-red" id="invoiceDiscTotal">− ۰ ت</span></div>
                            <div class="invoice-summary-divider"></div>
                            <div class="invoice-summary-row grand-total"><span>مبلغ قابل پرداخت</span><span><span
                                        id="invoiceFinalTotal">۰</span><span
                                        style="font-size:13px;color:var(--text-mute);"> تومان</span></span></div>
                            <div class="invoice-summary-words" id="invoiceTotalWords">صفر تومان</div>
                        </div>

                        <div class="invoice-footer-block">
                            <div
                                style="display:flex; justify-content:space-between; align-items:center; flex-wrap:wrap; gap:12px; margin-top:12px; padding:12px 0; border-top:1px solid var(--border-light);">
                                <div
                                    style="display:flex; align-items:center; gap:8px; background:var(--lavender); padding:8px 16px; border-radius:10px;">
                                    <i class="fa-regular fa-clock" style="color:var(--purple);"></i>
                                    <span style="font-weight:600; font-size:13px;">تاریخ و ساعت پرداخت: <strong
                                            id="invoicePaymentDateTime" style="color:var(--purple);">—</strong></span>
                                </div>
                                <div style="display:flex; gap:8px; flex-wrap:wrap;">
                                    <button class="step-prev-btn" data-prev="3"
                                        style="margin:0;background:var(--bg);color:var(--text-mid);border:1.5px solid var(--border);"><i
                                            class="fa-solid fa-chevron-right"></i> قبلی</button>
                                </div>
                            </div>

                            <div class="invoice-actions" style="margin-top:16px;">
                                <div class="terms-box">
                                    <input type="checkbox" id="termsChk" checked />
                                    <label for="termsChk">
                                        با خرید این دوره‌ها، <a href="#"
                                            onclick="showToast('قوانین و مقررات', 'info'); return false;">قوانین و
                                            مقررات</a> آموزشگاه پیشتاز را مطالعه کرده و می‌پذیرم.
                                    </label>
                                </div>
                            </div>

                            <div class="invoice-final-pay-row">
                                <button class="os-pay-btn invoice-final-pay-btn" id="finalPayBtn">
                                    <i class="fa-solid fa-lock"></i>
                                    <span id="finalPayLabel">پرداخت نهایی و تکمیل سفارش</span>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

        </main>

        <!-- SIDEBAR -->
        <aside class="checkout-sidebar" id="checkoutSidebar">
            <div class="order-summary">
                <div class="os-head">
                    <div class="os-head-ico"><i class="fa-solid fa-bag-shopping"></i></div>
                    <div>
                        <div class="os-head-title">خلاصه سفارش</div>
                        <div class="os-head-sub">فاکتور #PTZ-1404-0087</div>
                    </div>
                </div>
                <div class="os-items" id="sidebarItems"></div>
                <div class="os-breakdown">
                    <div class="os-line"><span class="osl-label">قیمت اصلی:</span><span class="osl-val"
                            style="text-decoration:line-through;color:#cbd5e1;" id="sideOrigPrice">۰ ت</span></div>
                    <div class="os-line" id="sideCouponLine" style="display:none;"><span class="osl-label"><i
                                class="fa-solid fa-ticket"></i> کد تخفیف:</span><span class="osl-red"
                            id="sideCouponDisc">—</span></div>
                    <div class="os-line"><span class="osl-label">تخفیف دوره‌ها:</span><span class="osl-red"
                            id="sideDisc">− ۰ ت</span></div>
                    <div class="os-divider"></div>
                </div>
                <div class="os-total">
                    <div>
                        <div class="ost-label">مبلغ قابل پرداخت</div>
                        <div class="ost-sub" id="sideTotalWords">صفر تومان</div>
                    </div>
                    <div style="text-align:left;">
                        <div class="ost-amt" id="sideTotalAmount">۰</div>
                        <div class="ost-unit">تومان</div>
                    </div>
                </div>
                <div class="os-saving"><i class="fa-solid fa-piggy-bank"></i> در این خرید <strong
                        id="sideSaving">۰</strong> تومان صرفه‌جویی کردید!</div>
            </div>
        </aside>
    </div>

    <!-- FOOTER -->
    <footer class="footer">
        <div class="footer-overlay"></div>
        <div class="footer-content">
            <div class="footer-col">
                <h3>دوره‌ها</h3>
                <ul>
                    <li><a href="#" onclick="showToast('فتوشاپ و طراحی', 'info'); return false;">فتوشاپ و طراحی</a></li>
                    <li><a href="#" onclick="showToast('پریمیر و تدوین', 'info'); return false;">پریمیر و تدوین</a></li>
                    <li><a href="#" onclick="showToast('افتر افکت و موشن', 'info'); return false;">افتر افکت و موشن</a>
                    </li>
                    <li><a href="#" onclick="showToast('هوش مصنوعی‌ها', 'info'); return false;">هوش مصنوعی‌ها</a></li>
                    <li><a href="#" onclick="showToast('ادیت با موبایل', 'info'); return false;">ادیت با موبایل</a></li>
                </ul>
            </div>
            <div class="footer-col center">
                <h2>MOHSENGFX.IR</h2>
                <p><a href="#" onclick="showToast('بازگشت به صفحه اصلی', 'info'); return false;">بازگشت به صفحه اصلی</a>
                </p>
                <div class="socials">
                    <a href="#" onclick="showToast('یوتیوب', 'info'); return false;"><i
                            class="fa-brands fa-youtube"></i></a>
                    <a href="#" onclick="showToast('تلگرام', 'info'); return false;"><i
                            class="fa-brands fa-telegram"></i></a>
                    <a href="#" onclick="showToast('اینستاگرام', 'info'); return false;"><i
                            class="fa-brands fa-instagram"></i></a>
                </div>
            </div>
            <div class="footer-col">
                <h3>لینک‌های مهم</h3>
                <ul>
                    <li><a href="#" onclick="showToast('دوره‌های پروژه محور', 'info'); return false;">دوره‌های پروژه
                            محور</a></li>
                    <li><a href="#" onclick="showToast('فایل لایه باز', 'info'); return false;">فایل لایه باز</a></li>
                    <li><a href="#" onclick="showToast('جعبه ابزار طراحی و ادیت', 'info'); return false;">جعبه ابزار
                            طراحی و ادیت</a></li>
                    <li><a href="#" onclick="showToast('نمونه کارهای هنرجویان', 'info'); return false;">نمونه کارهای
                            هنرجویان</a></li>
                    <li><a href="#" onclick="showToast('تماس با ما', 'info'); return false;">تماس با ما</a></li>
                </ul>
            </div>
        </div>
    </footer>

    <!-- MODAL & TOAST -->
    <div class="modal-ov" id="modalOv">
        <div class="modal-box">
            <div class="modal-circle"><i class="fa-solid fa-circle-check"></i></div>
            <h2>پرداخت با موفقیت انجام شد!</h2>
            <p>خرید شما تأیید شد.<br />دسترسی به دوره‌ها از طریق پنل کاربری فعال می‌باشد.</p>
            <div class="track-code-box">
                <div class="track-code-label">کد پیگیری سفارش</div><strong class="track-code-val"
                    id="trackCode">PTZ-1404-00087</strong>
            </div>
            <div class="modal-actions">
                <button class="modal-cta-primary" onclick="goToPanel()"><i class="fa-solid fa-graduation-cap"></i> پنل
                    کاربری</button>
                <button class="modal-cta-sec" onclick="closeModal()">بستن</button>
            </div>
            <div class="modal-note"><i class="fa-solid fa-envelope"></i> پیامک تأییدیه به شماره ثبت‌شده ارسال شد</div>
        </div>
    </div>
    <div class="toast-container" id="toastContainer"></div>

    <!-- SCRIPTS -->
    <script src="https://cdn.jsdelivr.net/npm/jquery/dist/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/persian-date/dist/persian-date.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/persian-datepicker@1.2.0/dist/js/persian-datepicker.min.js"></script>

    <script>
        (function () {
            'use strict';

            const $ = (id) => document.getElementById(id);
            const $$ = (s) => document.querySelectorAll(s);
            const jq = window.jQuery;

            // ---- STATE ----
            const S = {
                method: 'offline',
                gateway: 'zarinpal',
                couponApplied: false,
                couponAmt: 512000,
                searchOpen: false,
                isStep1Valid: false,
                isPaymentMethodSelected: true,
                maxQty: 50,
            };
            const COUPONS = ['MEHR1404', 'PISHTAZ50', 'WELCOME20', 'NOWRUZ25', 'VIP10', 'PHOTO10', 'PTZ20'];

            // ---- HELPERS ----
            function toman(n) { return n.toLocaleString('fa-IR'); }

            function numberToWords(n) {
                const units = ['', 'یک', 'دو', 'سه', 'چهار', 'پنج', 'شش', 'هفت', 'هشت', 'نه'];
                const teens = ['ده', 'یازده', 'دوازده', 'سیزده', 'چهارده', 'پانزده', 'شانزده', 'هفده', 'هجده', 'نوزده'];
                const tens = ['', 'ده', 'بیست', 'سی', 'چهل', 'پنجاه', 'شصت', 'هفتاد', 'هشتاد', 'نود'];
                const scales = ['', 'هزار', 'میلیون', 'میلیارد'];
                if (n === 0) return 'صفر';
                let parts = [];
                let num = Math.floor(n);
                let scaleIdx = 0;
                while (num > 0) {
                    let chunk = num % 1000;
                    if (chunk > 0) {
                        let chunkStr = '';
                        let h = Math.floor(chunk / 100);
                        let r = chunk % 100;
                        if (h > 0) {
                            chunkStr += h === 1 ? 'صد' : units[h] + 'صد';
                            if (r > 0) chunkStr += ' و ';
                        }
                        if (r > 0) {
                            if (r < 10) chunkStr += units[r];
                            else if (r < 20) chunkStr += teens[r - 10];
                            else {
                                let t = Math.floor(r / 10);
                                let u = r % 10;
                                chunkStr += tens[t];
                                if (u > 0) chunkStr += ' و ' + units[u];
                            }
                        }
                        if (scaleIdx > 0) chunkStr += ' ' + scales[scaleIdx];
                        parts.unshift(chunkStr);
                    }
                    num = Math.floor(num / 1000);
                    scaleIdx++;
                }
                return parts.join(' و ');
            }

            function getPersianDate() {
                if (typeof persianDate !== 'undefined') {
                    const pd = new persianDate();
                    pd.formatPersian = false;
                    const date = pd.format('YYYY/MM/DD');
                    const time = pd.format('HH:mm');
                    return { date, time, full: `${date} - ${time}` };
                }
                const now = new Date();
                const y = now.getFullYear();
                const mo = String(now.getMonth() + 1).padStart(2, '0');
                const d = String(now.getDate()).padStart(2, '0');
                const h = String(now.getHours()).padStart(2, '0');
                const mi = String(now.getMinutes()).padStart(2, '0');
                return { date: `${y}/${mo}/${d}`, time: `${h}:${mi}`, full: `${y}/${mo}/${d} - ${h}:${mi}` };
            }

            // ---- TOAST ----
            window.showToast = function (msg, type = 'info') {
                const c = $('toastContainer');
                if (!c) return;
                const t = document.createElement('div');
                const cls = { error: 't-error', success: 't-success', info: '' };
                t.className = 'toast ' + (cls[type] || '');
                const ico = { error: 'fa-circle-exclamation', success: 'fa-circle-check', info: 'fa-circle-info' };
                t.innerHTML = `<i class="fa-solid ${ico[type] || ico.info}"></i><span>${msg}</span>`;
                c.appendChild(t);
                requestAnimationFrame(() => requestAnimationFrame(() => t.classList.add('show')));
                setTimeout(() => {
                    t.classList.remove('show');
                    t.addEventListener('transitionend', () => t.remove(), { once: true });
                }, 3200);
            };

            // ---- COPY ----
            window.copyText = function (text, label) {
                navigator.clipboard.writeText(text).then(() =>
                    window.showToast(`<i class="fa-solid fa-copy"></i> ${label || ''} کپی شد`, 'success')
                );
            };

            // ---- SEARCH ----
            const searchToggle = $('searchToggle');
            const searchInput = $('searchInput');
            const searchClose = $('searchClose');
            if (searchToggle && searchInput && searchClose) {
                searchToggle.addEventListener('click', () => {
                    S.searchOpen = !S.searchOpen;
                    searchInput.classList.toggle('active', S.searchOpen);
                    searchClose.classList.toggle('show', S.searchOpen);
                    if (S.searchOpen) setTimeout(() => searchInput.focus(), 100);
                });
                searchClose.addEventListener('click', () => {
                    S.searchOpen = false;
                    searchInput.classList.remove('active');
                    searchClose.classList.remove('show');
                    searchInput.value = '';
                });
                searchInput.addEventListener('keydown', (e) => {
                    if (e.key === 'Enter') { const q = searchInput.value.trim(); if (q) window.showToast(`جستجو: "${q}"`, 'info'); }
                    if (e.key === 'Escape') searchClose.click();
                });
            }

            // ---- METHOD TABS ----
            $$('#methodTabs .tab-btn').forEach((btn) => {
                btn.addEventListener('click', function () {
                    $$('#methodTabs .tab-btn').forEach((b) => b.classList.remove('active'));
                    $$('.panel').forEach((p) => p.classList.remove('active'));
                    this.classList.add('active');
                    S.method = this.dataset.method;
                    $('panel-' + S.method).classList.add('active');
                    S.isPaymentMethodSelected = true;
                    updateAll();
                });
            });

            // ---- GATEWAY ----
            $$('#gatewayGrid .gw-opt').forEach((opt) => {
                opt.addEventListener('click', function () {
                    $$('#gatewayGrid .gw-opt').forEach((o) => o.classList.remove('chosen'));
                    this.classList.add('chosen');
                    this.querySelector('input[type="radio"]').checked = true;
                    S.gateway = this.dataset.gw;
                    S.isPaymentMethodSelected = true;
                });
            });

            // ---- FILE UPLOAD ----
            const uploadBox = $('uploadBox');
            const receiptFile = $('receiptFile');
            const previewImg = $('previewImg');
            const removeFileBtn = $('removeFileBtn');
            if (uploadBox) {
                uploadBox.addEventListener('click', () => { if (receiptFile) receiptFile.click(); });
            }
            if (receiptFile) {
                receiptFile.addEventListener('change', function () {
                    const file = this.files[0];
                    if (!file) return;
                    const r = new FileReader();
                    r.onload = (e) => {
                        if (previewImg) { previewImg.src = e.target.result; previewImg.style.display = 'block'; }
                        if (removeFileBtn) removeFileBtn.style.display = 'inline-block';
                        if (uploadBox) uploadBox.style.display = 'none';
                        const errEl = $('receiptFile-err');
                        if (errEl) errEl.style.display = 'none';
                        checkOfflineValidation();
                    };
                    r.readAsDataURL(file);
                });
            }
            if (removeFileBtn) {
                removeFileBtn.addEventListener('click', () => {
                    if (receiptFile) receiptFile.value = '';
                    if (previewImg) { previewImg.style.display = 'none'; previewImg.src = ''; }
                    if (removeFileBtn) removeFileBtn.style.display = 'none';
                    if (uploadBox) uploadBox.style.display = 'block';
                    checkOfflineValidation();
                });
            }

            // ---- CART DATA ----
            let cartItems = [
                { id: 1, name: 'دوره جامع کنکور ریاضی', duration: '۱۲۰ ساعت', origPrice: 4800000, discount: 30, finalPrice: 3360000, img: 'https://images.pexels.com/photos/5212340/pexels-photo-5212340.jpeg?auto=compress&cs=tinysrgb&fit=crop&h=80&w=120', instructor: 'استاد عباسی‌راد', qty: 1 },
                { id: 2, name: 'آمار و احتمال کنکور', duration: '۴۰ ساعت', origPrice: 2200000, discount: 20, finalPrice: 1760000, img: 'https://images.unsplash.com/photo-1626785774573-4b799315345d?w=120&h=80&fit=crop', instructor: 'استاد رضایی', qty: 1 },
                { id: 3, name: 'دوره رایگان مقدماتی', duration: '۱۰ ساعت', origPrice: 1500000, discount: 100, finalPrice: 0, img: 'https://images.unsplash.com/photo-1516321318423-f06f85e504b3?w=120&h=80&fit=crop', instructor: 'استاد رایگان‌پور', qty: 1 },
                { id: 4, name: 'دوره بدون تخفیف', duration: '۳۰ ساعت', origPrice: 2500000, discount: 0, finalPrice: 2500000, img: 'https://images.unsplash.com/photo-1524178232363-1fb2b075b655?w=120&h=80&fit=crop', instructor: 'استاد ثابت‌قیمت', qty: 1 },
            ];

            // ---- RENDER FUNCTIONS ----
            function renderCart() {
                const tbody = $('cartBody');
                if (!tbody) return;
                if (cartItems.length === 0) {
                    tbody.innerHTML = `<tr><td colspan="5" style="text-align:center;padding:20px;color:var(--text-mute);">سبد خرید خالی است</td></tr>`;
                } else {
                    tbody.innerHTML = cartItems.map((item) => {
                        const isFree = item.finalPrice === 0;
                        const hasDiscount = item.discount > 0 && !isFree;
                        const discountDisplay = hasDiscount ? `${item.discount}%` : '۰%';
                        return `
              <tr>
                <td>
                  <div style="display:flex;align-items:center;gap:10px;">
                    <img src="${item.img}" class="course-img" alt="${item.name}" loading="lazy" />
                    <div>
                      <div class="course-name">${item.name}</div>
                      <div class="course-meta">${item.instructor}</div>
                    </div>
                  </div>
                </td>
                <td>
                  <span class="price-orig-sm">${toman(item.origPrice)} ت</span>
                </td>
                <td>
                  <span class="discount-badge ${!hasDiscount ? 'zero' : ''}">${discountDisplay}</span>
                </td>
                <td>
                  ${isFree ? '<span class="price-final-sm free">رایگان</span>' : `<span class="price-final-sm">${toman(item.finalPrice)} ت</span>`}
                </td>
                <td>
                  <button class="remove-item-btn" data-id="${item.id}"><i class="fa-solid fa-trash-can"></i></button>
                </td>
              </tr>
            `;
                    }).join('');
                }
                updateAll();
            }

            function updateAll() {
                updateCartTotal();
                updateSidebar();
                updateInvoiceFinal();
            }

            function updateCartTotal() {
                const total = cartItems.reduce((sum, item) => sum + item.finalPrice * item.qty, 0);
                const el = $('cartTotal');
                if (el) el.textContent = toman(total);
            }

            function updateSidebar() {
                const container = $('sidebarItems');
                if (!container) return;
                if (cartItems.length === 0) {
                    container.innerHTML = '<p style="text-align:center;color:var(--text-mute);font-size:12px;padding:8px 0;">سبد خرید خالی است</p>';
                } else {
                    container.innerHTML = cartItems.map((item) => {
                        const isFree = item.finalPrice === 0;
                        return `
              <div class="os-item">
                <img src="${item.img}" class="os-thumb" alt="" loading="lazy" />
                <span class="os-cname">${item.name}</span>
                <span class="os-cprice ${isFree ? 'free' : ''}">${isFree ? 'رایگان' : toman(item.finalPrice * item.qty) + ' ت'}</span>
              </div>
            `;
                    }).join('');
                }

                const totalOrig = cartItems.reduce((s, i) => s + i.origPrice * i.qty, 0);
                const totalFinal = cartItems.reduce((s, i) => s + i.finalPrice * i.qty, 0);
                const totalDisc = totalOrig - totalFinal;
                const disc = S.couponApplied ? S.couponAmt : 0;
                const finalPay = totalFinal - disc;

                $('sideOrigPrice').textContent = toman(totalOrig) + ' ت';
                const totalDiscAll = totalDisc + disc;
                let discText = `− ${toman(totalDiscAll)} ت`;
                if (S.couponApplied && S.couponAmt) {
                    discText += ` <span style="font-size:10px;color:var(--text-mute);">(شامل تخفیف کد: − ${toman(S.couponAmt)} ت)</span>`;
                }
                $('sideDisc').innerHTML = discText;

                const couponLine = $('sideCouponLine');
                if (couponLine) couponLine.style.display = 'none';

                $('sideTotalAmount').textContent = toman(finalPay);
                $('sideTotalWords').textContent = numberToWords(finalPay) + ' تومان';
                $('sideSaving').textContent = toman(totalDisc + (S.couponApplied ? S.couponAmt : 0));
            }

            function updateInvoiceFinal() {
                const now = getPersianDate();
                $('invoiceNumber').textContent = 'PTZ-' + Date.now().toString().slice(-8);
                $('invoicePaymentDateTime').textContent = now.full;

                $('finalFname').textContent = $('fname').value || '—';
                $('finalLname').textContent = $('lname').value || '—';
                $('finalPhone').textContent = $('phone').value || '—';
                $('finalEmail').textContent = $('email').value || '—';

                const methodMap = { offline: 'کارت به کارت', online: 'پرداخت آنلاین' };
                const methodText = methodMap[S.method] || 'کارت به کارت';

                const receiptInfo = $('finalReceiptInfo');
                let items = [];
                items.push(`<div><span>روش:</span> <span>${methodText}</span></div>`);
                if (S.method === 'offline') {
                    const date = $('offDate')?.value || '—';
                    const time = $('offTime')?.value || '—';
                    const ref = $('offRef')?.value || '—';
                    items.push(`<div><span>تاریخ واریز:</span> <span>${date}</span></div>`);
                    items.push(`<div><span>ساعت واریز:</span> <span>${time}</span></div>`);
                    items.push(`<div><span>کد پیگیری:</span> <span>${ref}</span></div>`);
                } else {
                    items.push(`<div><span>تاریخ واریز:</span> <span>—</span></div>`);
                    items.push(`<div><span>ساعت واریز:</span> <span>—</span></div>`);
                    items.push(`<div><span>کد پیگیری:</span> <span>—</span></div>`);
                }
                receiptInfo.innerHTML = items.join('');

                const tbody = $('finalInvoiceItems');
                if (!tbody) return;
                if (cartItems.length === 0) {
                    tbody.innerHTML = `<tr><td colspan="3" style="text-align:center;padding:16px;color:var(--text-mute);">سبد خرید خالی است</td></tr>`;
                } else {
                    tbody.innerHTML = cartItems.map((item) => {
                        const isFree = item.finalPrice === 0;
                        const hasDiscount = item.discount > 0 && !isFree;
                        const courseDisplay = item.name + (item.instructor ? ` — ${item.instructor}` : '');
                        const qtyDisplay = item.qty > 1 ? ` ×${item.qty}` : '';
                        return `
              <tr>
                <td style="text-align:right;font-weight:700;font-size:13px;">
                  ${courseDisplay}${qtyDisplay}
                </td>
                <td>
                  ${hasDiscount || isFree ? `<span class="inv-price-orig">${toman(item.origPrice)} ت</span>` : `<span>${toman(item.origPrice)} ت</span>`}
                </td>
                <td>
                  <span class="inv-price-final ${isFree ? 'free' : ''}">${isFree ? 'رایگان' : toman(item.finalPrice * item.qty) + ' ت'}</span>
                </td>
              </tr>
            `;
                    }).join('');
                }

                const totalOrig = cartItems.reduce((s, i) => s + i.origPrice * i.qty, 0);
                const totalFinal = cartItems.reduce((s, i) => s + i.finalPrice * i.qty, 0);
                const totalDisc = totalOrig - totalFinal;
                const disc = S.couponApplied ? S.couponAmt : 0;
                const finalPay = totalFinal - disc;

                $('invoiceOrigTotal').textContent = toman(totalOrig) + ' ت';
                const totalDiscAll = totalDisc + disc;
                let discText = `− ${toman(totalDiscAll)} ت`;
                if (S.couponApplied && S.couponAmt) {
                    discText += ` <span style="font-size:11px;color:var(--text-mute);">(شامل تخفیف کد: − ${toman(S.couponAmt)} ت)</span>`;
                }
                $('invoiceDiscTotal').innerHTML = discText;
                $('invoiceFinalTotal').textContent = toman(finalPay);
                $('invoiceTotalWords').textContent = numberToWords(finalPay) + ' تومان';

                const couponRow = $('invoiceCouponRow');
                if (couponRow) couponRow.style.display = 'none';
            }

            // ---- CART EVENTS ----
            document.addEventListener('click', function (e) {
                const btn = e.target.closest('.remove-item-btn');
                if (!btn) return;
                const id = parseInt(btn.dataset.id);
                cartItems = cartItems.filter((item) => item.id !== id);
                renderCart();
                window.showToast('محصول از سبد خرید حذف شد.', 'info');
            });

            $('addSampleBtn')?.addEventListener('click', function () {
                const sample = {
                    id: Date.now(),
                    name: 'دوره نمونه جدید',
                    duration: '۲۰ ساعت',
                    origPrice: 1200000,
                    discount: 15,
                    finalPrice: 1020000,
                    img: 'https://images.unsplash.com/photo-1626785774573-4b799315345d?w=120&h=80&fit=crop',
                    instructor: 'استاد نمونه',
                    qty: 1,
                };
                cartItems.push(sample);
                renderCart();
                window.showToast('دوره نمونه به سبد خرید اضافه شد.', 'success');
            });

            // ---- COUPON ----
            const couponInput = $('couponInput');
            const couponBtn = $('couponBtn');
            const couponBtnTxt = $('couponBtnTxt');
            const couponOk = $('couponOk');
            const couponErr = $('couponErr');

            function shakeEl(el) {
                if (!el) return;
                el.classList.add('shake');
                el.addEventListener('animationend', () => el.classList.remove('shake'), { once: true });
            }

            function applyCoupon() {
                if (S.couponApplied) return;
                if (!couponInput) return;
                const code = couponInput.value.trim().toUpperCase();
                if (couponOk) couponOk.classList.remove('show');
                if (couponErr) couponErr.classList.remove('show');
                if (!code) { shakeEl(couponInput); return; }
                if (couponBtnTxt) couponBtnTxt.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> بررسی...';
                if (couponBtn) couponBtn.disabled = true;

                setTimeout(() => {
                    if (COUPONS.includes(code)) {
                        S.couponApplied = true;
                        if (couponOk) {
                            couponOk.innerHTML = `
                <i class="fa-solid fa-circle-check"></i> کد <strong>${code}</strong> اعمال شد — <strong>${toman(S.couponAmt)}</strong> تومان تخفیف گرفتید!
                <button class="cancel-coupon-btn" onclick="cancelCoupon()">لغو کد</button>
              `;
                            couponOk.classList.add('show');
                        }
                        if (couponInput) { couponInput.disabled = true; couponInput.style.opacity = '0.55'; }
                        if (couponBtnTxt) couponBtnTxt.innerHTML = 'اعمال شد';
                        if (couponBtn) couponBtn.style.cursor = 'default';
                        updateAll();
                        window.showToast('کد تخفیف با موفقیت اعمال شد! 🎉', 'success');
                    } else {
                        if (couponErr) couponErr.classList.add('show');
                        shakeEl(couponInput);
                        if (couponBtnTxt) couponBtnTxt.innerHTML = '<i class="fa-solid fa-check-circle"></i> اعمال';
                        if (couponBtn) couponBtn.disabled = false;
                    }
                }, 800);
            }
            window.applyCoupon = applyCoupon;

            window.cancelCoupon = function (showToastMsg = true) {
                S.couponApplied = false;
                S.couponAmt = 512000;
                if (couponInput) { couponInput.disabled = false; couponInput.style.opacity = '1'; couponInput.value = ''; }
                if (couponBtnTxt) couponBtnTxt.innerHTML = '<i class="fa-solid fa-check-circle"></i> اعمال';
                if (couponBtn) { couponBtn.disabled = false; couponBtn.style.cursor = 'pointer'; }
                if (couponOk) { couponOk.classList.remove('show'); couponOk.innerHTML = ''; }
                updateAll();
            };

            if (couponInput) {
                couponInput.addEventListener('keydown', (e) => { if (e.key === 'Enter') applyCoupon(); });
                couponInput.addEventListener('input', () => {
                    if (!S.couponApplied) {
                        if (couponOk) couponOk.classList.remove('show');
                        if (couponErr) couponErr.classList.remove('show');
                    }
                });
            }
            if (couponBtn) couponBtn.addEventListener('click', applyCoupon);

            // ---- STEP 1 VALIDATION ----
            const STEP1_FIELDS = [
                { id: 'fname', errId: 'fname-err', test: (v) => v.trim().length >= 2 },
                { id: 'lname', errId: 'lname-err', test: (v) => v.trim().length >= 2 },
                { id: 'phone', errId: 'phone-err', test: (v) => /^09\d{9}$/.test(v.trim()) },
            ];

            function validateStep1Field(el, errId, test) {
                const val = el.value;
                const ok = test(val);
                if (val.trim().length > 0) {
                    el.classList.toggle('invalid', !ok);
                    el.classList.toggle('valid', ok);
                    const e = $(errId);
                    if (e) e.classList.toggle('show', !ok);
                } else {
                    el.classList.remove('invalid', 'valid');
                    const e = $(errId);
                    if (e) e.classList.remove('show');
                }
                return ok;
            }

            STEP1_FIELDS.forEach(({ id, errId, test }) => {
                const el = $(id);
                if (!el) return;
                if (id === 'phone') {
                    el.addEventListener('input', () => {
                        el.value = el.value.replace(/[^0-9]/g, '').substring(0, 11);
                    });
                }
                el.addEventListener('input', () => {
                    validateStep1Field(el, errId, test);
                    checkStep1Validation();
                });
                el.addEventListener('blur', () => {
                    validateStep1Field(el, errId, test);
                    checkStep1Validation();
                });
            });

            function validateAllStep1() {
                let ok = true;
                STEP1_FIELDS.forEach(({ id, errId, test }) => {
                    const el = $(id);
                    if (el) {
                        const valid = validateStep1Field(el, errId, test);
                        if (!valid) ok = false;
                    }
                });
                return ok;
            }

            function checkStep1Validation() {
                const isValid = validateAllStep1();
                S.isStep1Valid = isValid;
                const nextBtn = $('step1NextBtn');
                if (nextBtn) nextBtn.disabled = !isValid;
                document.querySelectorAll('.step-header .cstep').forEach((el) => {
                    const step = parseInt(el.dataset.step);
                    if (step > 1) el.classList.toggle('disabled', !isValid);
                });
            }

            // ---- OFFLINE VALIDATION ----
            const OFFLINE_REQUIRED = ['offDate', 'offTime'];

            function validateOfflineField(el, errId) {
                if (!el) return;
                const val = el.value;
                const isValid = val.trim().length > 0;
                if (val.trim().length > 0) {
                    el.classList.toggle('invalid', !isValid);
                    el.classList.toggle('valid', isValid);
                    const e = $(errId);
                    if (e) e.classList.toggle('show', !isValid);
                } else {
                    el.classList.remove('invalid', 'valid');
                    const e = $(errId);
                    if (e) e.classList.remove('show');
                }
                return isValid;
            }

            OFFLINE_REQUIRED.forEach((id) => {
                const el = $(id);
                if (!el) return;
                const errId = id + '-err';
                el.addEventListener('input', function () { validateOfflineField(this, errId); });
                el.addEventListener('blur', function () { validateOfflineField(this, errId); });
                // پشتیبانی از فلش‌های بالا/پایین
                el.addEventListener('keydown', function (e) {
                    if (e.key === 'ArrowUp' || e.key === 'ArrowDown') {
                        e.preventDefault();
                        let val = this.value.trim();
                        if (!val) val = '00:00';
                        // فقط برای ساعت (offTime) کار می‌کند
                        if (this.id === 'offTime') {
                            let parts = val.split(':');
                            let h = parseInt(parts[0]) || 0;
                            let m = parseInt(parts[1]) || 0;
                            if (e.key === 'ArrowUp') {
                                h = (h + 1) % 24;
                            } else {
                                h = (h - 1 + 24) % 24;
                            }
                            this.value = String(h).padStart(2, '0') + ':' + String(m).padStart(2, '0');
                            validateOfflineField(this, errId);
                        }
                        // برای تاریخ (offDate) فلش‌ها روز را تغییر دهند
                        if (this.id === 'offDate') {
                            let pd = new persianDate();
                            if (val) {
                                const parts2 = val.split('/');
                                if (parts2.length === 3) {
                                    pd.year(parseInt(parts2[0]));
                                    pd.month(parseInt(parts2[1]) - 1);
                                    pd.date(parseInt(parts2[2]));
                                }
                            }
                            if (e.key === 'ArrowUp') {
                                pd.addDay(1);
                            } else {
                                pd.addDay(-1);
                            }
                            this.value = pd.format('YYYY/MM/DD');
                            validateOfflineField(this, errId);
                        }
                    }
                });
            });

            function validateOfflineFields() {
                let ok = true;
                OFFLINE_REQUIRED.forEach((id) => {
                    const el = $(id);
                    const errId = id + '-err';
                    if (!el) return;
                    const isValid = validateOfflineField(el, errId);
                    if (!isValid) ok = false;
                });
                const fileInput = $('receiptFile');
                const fileErr = $('receiptFile-err');
                const hasFile = fileInput && fileInput.files && fileInput.files.length > 0;
                if (fileErr) fileErr.style.display = hasFile ? 'none' : 'block';
                if (!hasFile) ok = false;
                return ok;
            }

            function checkOfflineValidation() {
                if (S.method !== 'offline') return true;
                return validateOfflineFields();
            }

            const offRef = $('offRef');
            if (offRef) {
                offRef.addEventListener('input', function () {
                    this.value = this.value.replace(/[^۰-۹0-9A-Za-z]/g, '').substring(0, 30);
                    if (this.value.trim().length > 0) {
                        this.classList.remove('invalid');
                        this.classList.add('valid');
                    } else {
                        this.classList.remove('valid', 'invalid');
                    }
                });
            }

            // ---- STEP MANAGER ----
            window.goToStep = function (step) {
                if (step < 1 || step > 4) return;
                if (step > 1 && !S.isStep1Valid) {
                    window.showToast('لطفاً ابتدا اطلاعات کاربری را کامل کنید.', 'error');
                    validateAllStep1();
                    return;
                }
                if (step === 4) {
                    if (!S.isPaymentMethodSelected) {
                        window.showToast('لطفاً روش پرداخت را انتخاب کنید.', 'error');
                        return;
                    }
                    if (S.method === 'offline') {
                        const isValid = validateOfflineFields();
                        if (!isValid) {
                            window.showToast('لطفاً تمام فیلدهای اجباری (تاریخ، ساعت، فیش) را تکمیل کنید.', 'error');
                            return;
                        }
                    }
                }

                const sidebar = $('checkoutSidebar');
                const wrap = document.querySelector('.checkout-wrap');
                if (sidebar && wrap) {
                    if (step === 4) {
                        sidebar.classList.add('hidden');
                        wrap.classList.add('sidebar-hidden');
                    } else {
                        sidebar.classList.remove('hidden');
                        wrap.classList.remove('sidebar-hidden');
                    }
                }

                document.querySelectorAll('.step-panel').forEach((p) => p.classList.remove('active'));
                const panel = document.querySelector(`.step-panel[data-step="${step}"]`);
                if (panel) panel.classList.add('active');

                document.querySelectorAll('.step-header .cstep').forEach((el) => {
                    const s = parseInt(el.dataset.step);
                    el.classList.remove('active', 'done');
                    if (s === step) el.classList.add('active');
                    else if (s < step) el.classList.add('done');
                });

                window.scrollTo({ top: 0, behavior: 'smooth' });
                if (step === 4) updateInvoiceFinal();
            };

            document.addEventListener('click', function (e) {
                const nextBtn = e.target.closest('.step-next-btn');
                if (nextBtn) {
                    const next = parseInt(nextBtn.dataset.next);
                    if (!isNaN(next)) {
                        if (next === 4 && S.method === 'offline') {
                            const isValid = validateOfflineFields();
                            if (!isValid) {
                                window.showToast('لطفاً تمام فیلدهای واریز را تکمیل کنید و فیش را بارگذاری نمایید.', 'error');
                                return;
                            }
                        }
                        window.goToStep(next);
                    }
                }
                const prevBtn = e.target.closest('.step-prev-btn');
                if (prevBtn) {
                    const prev = parseInt(prevBtn.dataset.prev);
                    if (!isNaN(prev)) window.goToStep(prev);
                }
            });

            document.querySelectorAll('.step-header .cstep').forEach((el) => {
                el.addEventListener('click', function () {
                    const step = parseInt(this.dataset.step);
                    if (step === 1 || (step > 1 && S.isStep1Valid)) {
                        window.goToStep(step);
                    } else {
                        window.showToast('لطفاً ابتدا اطلاعات مرحله ۱ را کامل کنید.', 'error');
                    }
                });
            });

            // ---- MODAL ----
            window.closeModal = function () {
                const modal = $('modalOv');
                if (modal) modal.classList.remove('on');
            };
            window.goToPanel = function () {
                window.showToast('در حال انتقال به پنل کاربری...', 'success');
                window.closeModal();
            };

            const modalOv = $('modalOv');
            if (modalOv) {
                modalOv.addEventListener('click', (e) => { if (e.target === modalOv) window.closeModal(); });
            }
            document.addEventListener('keydown', (e) => {
                if (e.key === 'Escape') {
                    window.closeModal();
                    if (S.searchOpen && searchClose) searchClose.click();
                }
            });

            // ---- FINAL PAY ----
            $('finalPayBtn')?.addEventListener('click', function () {
                const chk = $('termsChk');
                if (!chk || !chk.checked) {
                    window.showToast('لطفاً قوانین را بپذیرید.', 'error');
                    return;
                }
                const modal = $('modalOv');
                if (modal) {
                    const tc = $('trackCode');
                    if (tc) tc.textContent = 'PTZ-' + Date.now().toString().slice(-8);
                    modal.classList.add('on');
                }
                window.showToast('پرداخت شما با موفقیت انجام شد!', 'success');
            });

            // ---- PERSIAN DATEPICKER (با قابلیت ورود دستی و فلش) ----
            (function initDatepickers() {
                const offDateInput = document.getElementById('offDate');
                const offTimeInput = document.getElementById('offTime');

                if (typeof jq === 'undefined' || !jq.fn || !jq.fn.pDatepicker) {
                    console.warn('persian-datepicker not loaded.');
                    return;
                }

                if (offDateInput) {
                    jq(offDateInput).pDatepicker({
                        format: 'YYYY/MM/DD',
                        autoClose: true,
                        initialValue: false,
                        observer: true,
                        onSelect: function () {
                            validateOfflineField(offDateInput, 'offDate-err');
                            if (document.querySelector('.step-panel[data-step="4"].active')) updateInvoiceFinal();
                        }
                    });
                    offDateInput.addEventListener('click', function (e) {
                        e.stopPropagation();
                        jq(offDateInput).pDatepicker('show');
                    });
                    const icon = offDateInput.parentElement?.querySelector('.date-picker-icon');
                    if (icon) {
                        icon.addEventListener('click', function (e) {
                            e.stopPropagation();
                            jq(offDateInput).pDatepicker('show');
                        });
                    }
                    offDateInput.addEventListener('input', function () {
                        validateOfflineField(this, 'offDate-err');
                    });
                }

                if (offTimeInput) {
                    jq(offTimeInput).pDatepicker({
                        onlyTimePicker: true,
                        format: 'HH:mm',
                        autoClose: true,
                        initialValue: false,
                        observer: true,
                        timePicker: {
                            enabled: true,
                            meridian: { enabled: false },
                            second: { enabled: false }
                        },
                        onSelect: function () {
                            validateOfflineField(offTimeInput, 'offTime-err');
                            if (document.querySelector('.step-panel[data-step="4"].active')) updateInvoiceFinal();
                        }
                    });
                    offTimeInput.addEventListener('click', function (e) {
                        e.stopPropagation();
                        jq(offTimeInput).pDatepicker('show');
                    });
                    const timeIcon = offTimeInput.parentElement?.querySelector('.time-picker-icon');
                    if (timeIcon) {
                        timeIcon.addEventListener('click', function (e) {
                            e.stopPropagation();
                            jq(offTimeInput).pDatepicker('show');
                        });
                    }
                    offTimeInput.addEventListener('input', function () {
                        validateOfflineField(this, 'offTime-err');
                    });
                }
            })();

            // ---- INIT ----
            renderCart();
            checkStep1Validation();
            window.goToStep(1);

        })();
    </script>

</body>

</html>